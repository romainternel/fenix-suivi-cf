# Régression — v286 (STORY-48 : Mode "Articulation" côté Attaque — clôture)

**Agent :** Regression Guardian
**Date :** 2026-09-16
**Changement :** `js/page-analyse.js` (fin de `renderEncFamillesSection` — dispatch articulation ajouté), `FENIX-HANDBALL-CF-SUIVI.html` (v286), `CLAUDE.md`, `docs/regression/checklist.md`

---

Ce rapport clôt le cycle STORY-48 ouvert par `docs/regression/v284.md` (verdict alors "AUDIT INCOMPLET" faute de MCP Playwright). Le serveur étant redevenu disponible dans ce même cycle, l'intégralité de la réserve a été levée par des clics réels sur la production.

## Bug réel trouvé et corrigé (v286)

Le premier test réel a révélé que basculer Attaque↔Défense (ou changer de match) pendant que le mode Articulation était déjà actif laissait `#enc-articulation-wrap` complètement vide — `renderEncFamillesSection()` reconstruit tout le HTML de la section sans jamais rappeler la fonction de dessin, seul `_setEncGraphMode()` le faisait. **Ce bug touchait aussi le mode Défense depuis STORY-34 (v258)**, jamais détecté car jamais testé dans cet ordre précis avant ce cycle. Corrigé en reproduisant le même dispatch qu'`_setEncGraphMode` à la fin de `renderEncFamillesSection` (commit `59f76cf`).

## Vérification finale (clics réels, v286 déployée)

- **Défense** (I22) : re-testée après correctif sur J01 BILLERE-FENIX — dispositif 0-6/1-5, largeur À6/À4/À2, terrain et classement identiques à l'existant. Non-régression confirmée par l'observation, pas seulement par `git diff`.
- **Attaque** (I30) : les 9 critères d'acceptation de STORY-48 revérifiés par clic réel (terrain peuplé, bascule largeur, panneau d'édition, override manuel reflété immédiatement, clic classement place la composition d'un coup, formule directe confirmée visuellement, cas vide rencontré et affiché proprement, non-régression défense, 2 matchs testés).
- **Critère 7 (Risk R5, le plus à risque)** : poste P3 ouvert côté Défense → aller-retour complet vers Attaque et retour → éditeur P3 resté ouvert, aucune réinitialisation, aucune confusion visuelle. Répété deux fois, résultat identique des deux côtés.
- **Changement de match pendant Articulation actif** : re-testé explicitement (scénario exact ayant révélé le bug) — bascule J01→J02 avec le correctif en place, terrain redessiné correctement pour J02.
- **Console navigateur** : zéro erreur, zéro avertissement sur toute la session de test (plusieurs vérifications `browser_console_messages`).
- **Checklist** : I22 et I30 mises à jour avec les résultats réels et la date du test (2026-09-16, v286).

## Verdict

**RAS** — l'audit incomplet signalé dans `docs/regression/v284.md` est clos : le seul point resté en suspens (vérification par clic réel, y compris le critère 7 le plus sensible) est maintenant fait, un bug réel trouvé au passage a été corrigé et re-testé. STORY-48 est **prête pour mise en production sans réserve**.
