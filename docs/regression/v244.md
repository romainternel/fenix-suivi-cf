# Régression — v244 (STORY-25 : Écran d'édition des familles tactiques)

**Agent :** Regression Guardian
**Date :** 2026-09-01
**Changement :** `FENIX-HANDBALL-CF-SUIVI.html` (entrée menu + panneau `#fam-modal`), `js/player-mode.js` (+1 branche dans le handler Escape), `js/page-analyse.js` (+7 fonctions, toutes nouvelles)

---

## Analyse de risque

Changement presque entièrement additif : la seule modification d'une fonction existante est l'ajout d'une branche `else if` dans le handler `Escape` (`js/player-mode.js`), qui ne change rien au comportement des panneaux déjà existants (`pa-modal`, `preview-modal`). Le reste est un nouveau panneau, de nouvelles fonctions, une nouvelle entrée de menu — surface de régression minimale.

## Vérification

- **I9 (Menu Outils)** : re-testé avec la 4e entrée — les 3 entrées existantes (Comptes joueurs, Vue joueur, Migrer mes données locales) toujours fonctionnelles, ouverture/fermeture du menu inchangée.
- **I1 (Page Analyse — familles)** : le test principal de cette story EST un test de non-régression numérique poussé — réassignation d'une vraie correspondance, vérification exacte du déplacement de possessions entre cartes, puis restauration à l'identique. Aucune divergence.
- **I7 (Comptes joueurs)** : non concerné par cette story (fonctions distinctes), non re-testé spécifiquement — aucun code partagé modifié.
- **Focus trap / Échap sur les panneaux existants** (`pa-modal`, `preview-modal`) : logique de la branche `Escape` inchangée pour ces deux cas (juste un `else if` ajouté après), pas de risque de régression sur leur comportement.

## Verdict

**RAS** — aucune régression détectée. Donnée réelle de production (`famille_mapping`) laissée strictement identique à son état d'avant les tests, vérifié via l'API REST.
