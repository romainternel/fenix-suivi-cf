# Régression — v245 (STORY-26 : Écran d'édition des bilans)

**Agent :** Regression Guardian
**Date :** 2026-09-01
**Changement :** `FENIX-HANDBALL-CF-SUIVI.html` (entrée menu + panneau `#bilan-modal`, +7 fonctions, tri ajouté dans `loadFromSupabase()`), `js/player-mode.js` (+1 branche Escape)

---

## Analyse de risque

Story presque entièrement additive, à une exception notable : un tri a été ajouté à la ligne existante `_rawBilanRows = ...` dans `loadFromSupabase()` (STORY-22). Ce changement mérite une vérification spécifique — il touche une fonction déjà critique du cycle (boot de l'app).

## Vérification

- **`loadFromSupabase()` / bilans au chargement normal** : le tri ajouté (`_sortRawBilanRows`) ne fait que réordonner un tableau déjà correct dans le cas standard (bilans déjà en ordre chronologique, ce qui est toujours le cas pour un import Excel) — testé que `processBilans()` produit un résultat identique à avant sur les données réelles (2 bilans, ordre déjà correct par construction).
- **I9 (Menu Outils)** : re-testé avec la 5e entrée — les 4 entrées existantes toujours fonctionnelles.
- **I1 (Page Analyse)** : non concerné par cette story, aucun code partagé modifié.
- **Page Notes / filtre Période** : re-testé avec les vrais bilans après restauration complète des données de test — comportement identique à avant la story.

## Verdict

**RAS** — aucune régression détectée. Donnée réelle de production (`bilan`) laissée strictement identique à son état d'avant les tests, vérifié via l'API REST.
