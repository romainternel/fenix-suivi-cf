# Checklist de régression — FENIX Stats CF

**Maintenu par :** Regression Guardian
**Créé le :** 2026-08-26 (première initialisation — aucune checklist n'existait avant ce fichier)
**Source de construction :** `CLAUDE.md` (historique commit), `docs/qa/QA-01`, `docs/qa/QA-02`, `docs/audit/AUDIT-NUIT-2026-06-16.md`, historique git (v1→v221), mémoire long-terme du projet.

> ⚠️ Aucun parcours listé ici n'avait jamais été testé dans un vrai navigateur avant l'audit du 2026-08-26 (voir `docs/regression/audit-complet-2026-08-26.md`). Les QA précédents (QA-01, QA-02) étaient faits par lecture de code uniquement.

Environnement de référence : https://romainternel.github.io/fenix-suivi-cf/FENIX-HANDBALL-CF-SUIVI.html (production, GitHub Pages). Donnée de test de référence : `ESSAI IA STAT.xlsm` (racine repo).

---

## Critique

| # | Feature | Introduite | Critère de bon fonctionnement (binaire) | Dernière vérif. OK |
|---|---------|------------|------------------------------------------|---------------------|
| C1 | Authentification Staff | v1, `checkLogin()` déclenche aussi la migration locale depuis v242 (STORY-23), devenue async en v243 (STORY-24) | Mot de passe `Partage` → accès au dashboard staff (pas d'écran joueur) | ✅ 2026-09-09 (audit-complet-2026-09-09, v277) |
| C2 | Authentification Joueur | v1, comptes migrés vers Supabase Auth en v243 (STORY-24, remplace `fenix_player_accounts`/localStorage) | Choix du nom dans le menu déroulant + mot de passe du compte Supabase Auth → bascule automatique en Mode Lecture Joueur avec le bon nom | ⚠️ NON RE-TESTÉ EN CONDITIONS RÉELLES cette session (pas de compte joueur actif sur le projet actuellement, cf. I7 — créer un compte de test aurait été une action d'écriture évitée par prudence). Vérifié indirectement via le panneau "Vue joueur" (I11, impersonation staff) qui bascule sur le même rendu Mode Lecture Joueur — dernier test réel du flux `signInWithPassword()` : 2026-09-01 (audit-complet-2026-09-01-soir, v248), code non modifié depuis |
| C3 | Import fichier Excel (.xlsm/.xlsx) | v1, synchronise vers Supabase depuis v240 (STORY-21), colonnes Articulation défensive ajoutées en v257 (STORY-33) | Sélection du fichier → `DATA`/`MATCHS`/`SAISONS`/`JOUEURS_TERRAIN` peuplés → dashboard affiche des stats non nulles | ⚠️ NON RE-TESTÉ cette session (action destructive — remplace entièrement `match_data`/`joueurs`/`tableau_match`/`bilan` en production — évitée par prudence, cf. mémoire "pas de backend de test"). Dashboard vérifié non vide et cohérent (4 matchs, stats réelles) confirmant que les données importées précédemment restent saines. Dernier réimport réel testé : 2026-09-02 (v257, 540 lignes/4 matchs, `docs/e2e/E2E-18-import-articulation-defensive.md`), code non modifié depuis. **Note qualité de donnée préexistante** (non liée à STORY-33) : le fichier Excel de Romain contient une ligne "legende" juste après l'en-tête réelle (libellés `"#1 Club"` etc.) qui passe le filtre d'import et apparaît comme un faux match "Rencontre" dans le sélecteur — à nettoyer côté fichier source, pas un bug applicatif |
| C4 | Dashboard staff | v1, source de données = Supabase au chargement depuis v241 (STORY-22, remplace `loadFromLocalStorage()`) | Filtre match/saison appliqué → encarts FENIX/Adversaire (possessions, buts, %, PB, Pen, Neutralisé) cohérents et non figés | ✅ 2026-09-09 (audit-complet-2026-09-09, v277) |
| C5 | Page Joueurs — terrain + fiche | v1, passe visuelle en v235 (STORY-17), source Supabase depuis v241 (STORY-22), photos joueurs depuis v250-254 (STORY-30/31/32) | Clic sur un joueur du terrain (ronds = photo portrait si disponible, sinon initiales, espacement gardien sans chevauchement) → panneau stats détaillées s'affiche avec les bonnes stats, ET bascule directe en photo corps entier grand format si disponible (pas de clic supplémentaire), avec bouton "↩ Terrain" pour revenir. Fiche principale en élévation "hero" (`#joueur-panel`), détail par match en "card" (`#joueur-matches`), sous-onglets Fiche/Notes/Graphique/Impact sur l'échelle typographique "Onglet" | ✅ 2026-09-09 (audit-complet-2026-09-09, v277 — terrain photo, bascule directe sans latence, fiche joueur de champ (Antonin Vache) ET fiche gardien (Gabin Saltel) re-vérifiées) |
| C6 | Mode Lecture Joueur (mobile) — Ma Fiche | v96+ (refonte sprint S-01→S-11, jusqu'à v221), passe visuelle en v236 (STORY-18), avatar photo depuis v250 (STORY-30) | Connexion joueur → onglet "Ma Fiche" affiche KPIs + graphique progression sans erreur, avatar avec photo portrait si disponible sinon initiales. Cartes (KPI/signature/ACTIONS) sur les mêmes tokens `--gray-*`/`--shadow-sm` que le reste de l'appli, onglets Ma Fiche/Stats Match/Impact sur l'échelle "Onglet", tableau détaillé ATT+/ATT-/DEF+/DEF- en fond clair/texte foncé | ✅ 2026-09-09 (audit-complet-2026-09-09, v277 — vérifié via impersonation "Vue joueur" sur un gardien réel, Enzo Ditta : Ma Fiche + zones Impact mobile re-vérifiées, 0 erreur console) |
| C7 | Persistance des filtres bilan/saison/match entre pages | historique — source de bugs récurrente (AUDIT-NUIT bug #5, BUG-22 v220) | Changer de page ne doit ni planter ni afficher des données d'un autre bilan sans avertissement | ✅ 2026-09-09 (audit-complet-2026-09-09, v277 — navigation Dashboard↔Analyse↔Joueurs↔Outils, aucun plantage, aucun état incohérent observé) |

## Important

| # | Feature | Introduite | Critère de bon fonctionnement (binaire) | Dernière vérif. OK |
|---|---------|------------|------------------------------------------|---------------------|
| I1 | Page Analyse (vue agrégée saison + enclenchements) | v1, découpée en onglets internes depuis v233 (STORY-14), regroupée 5→3/2 en v272 (STORY-40), bloc Essentiel ajouté en v271 (STORY-39), source Supabase depuis v241 (STORY-22), classification famille via `FAMILLE_MAPPING`/Supabase depuis v243 (STORY-24) ; barre de filtres sticky corrigée en v280 ; option CLUB "ADV GEN" ajoutée en v281 | Chargement de la vue "Saison complète" → bloc Essentiel (`#essentiel-saison`) + cartes GÉNÉRAL/ATT PLACÉE/GRAND ESPACE lisibles + onglets Tactique/Tendances. Avec un match sélectionné : bloc Essentiel (`#essentiel-match`) puis bloc terrain+cartes toujours visibles au-dessus des onglets Vue d'ensemble/Tactique/Notes & Outils, cartes de chaque onglet en `.surface-card`, chaque onglet correctement rendu dès sa première ouverture (canvas Timeline dans Vue d'ensemble et camembert/matrice dans Tactique redessinés à l'ouverture, jamais vides). Cards familles enclenchement : max 3 niveaux typographiques, drill-down fonctionnel. L'onglet "Tactique" (familles + Gardien) doit afficher les stats par famille adverse pour chaque gardien FENIX ayant joué, pas "Aucune donnée gardien." **Barre de filtres MATCH/CLUB/RÉSULTAT/GE (`#analyse-sticky-bar`, `position:sticky`)** : doit rester visible, collée juste sous le header, quelle que soit la profondeur de scroll sur la page — jamais masquée derrière le header ni disparue de l'écran. **Filtre CLUB (nuage de tirs uniquement, `terrainData` dans `updateMatchPage()`)** : options toujours dans l'ordre Tous/FENIX/**ADV GEN**/[adversaires réels triés], que ce soit en vue "Saison complète" (`populateFilters()`) ou un match précis sélectionné (`updateMatchClubFilter()`). "ADV GEN" (valeur sentinelle `ADV_GEN`, pas un vrai nom de club) affiche sur le terrain tous les tirs adverses confondus (tous clubs adverses réunis, aucun FENIX) — utile en vue saison où plusieurs adversaires coexistent, pour ne pas devoir les sélectionner un par un. Ne modifie pas les cartes GÉNÉRAL/ATT PLACÉE/GRAND ESPACE (déjà agrégées tous adversaires confondus indépendamment de ce filtre, `advAll` dans `updateMatchPage()`) | ✅ 2026-09-15 (v280 sticky bar, v281 ADV GEN — bug signalé par Romain, capture à l'appui : la barre de filtres MATCH/CLUB/RÉSULTAT/GE disparaissait entièrement dès qu'on scrollait la page (ex. après avoir déplié "Terrain & Comparatif"), au lieu de rester collée sous le header comme le nom "sticky" le suggère. Root-cause : `stickyBar.style.top` était fixé une seule fois à `DOMContentLoaded` sur `hdr.offsetHeight` (47px), *avant* que `populateFilters()` ne rende visible le `<select>` de saison dans le header (`display:none`→`block`, une fois `SAISONS` chargé depuis Supabase) — ce qui fait grandir le header à 80px après coup. La barre restait donc collée à l'ancien offset (47px), soit *derrière* le header agrandi (80px), invisible dès qu'on scrollait au-delà. Corrigé avec un `ResizeObserver` sur `.header` qui met à jour `top` en continu plutôt qu'une mesure figée. Vérifié en conditions réelles : `top` calculé passe de 47px à 80px, la barre reste visible et correctement positionnée après scroll, `docs/regression/screenshots/fix-analyse-sticky-filters.png`. Puis demande de Romain (capture du menu CLUB) : ajout de "ADV GEN" — vérifié en conditions réelles en vue saison (2 adversaires réels, BILLERE + LA CRAU, 173 tirs combinés, aucun FENIX dans `_lastTerrainData`) et en vue match précis (1 seul adversaire, résultat identique à le sélectionner par son nom), `docs/regression/screenshots/fix-club-filter-adv-gen.png`) |
| I10 | Onglets internes page Analyse | v233 (STORY-14) → regroupés 5→3 en vue match / 2 en vue saison en v272 (STORY-40) — remplace la structure à 5 onglets (Résumé/Timeline/Intention attaque/Gardien/Chat IA) décrite dans les rapports antérieurs à cette date | **Vue match** (3 onglets, `AN_TABS_MATCH`) : "Vue d'ensemble" (Indicateurs Clés + Timeline/Moments clés/Bascules), "Tactique" (familles + Gardien, dans cet ordre), "Notes & Outils" (Coach + Chat IA). **Vue saison** (2 onglets, `AN_TABS_SAISON`, nouveau — la vue saison n'avait aucun onglet avant STORY-40) : "Tactique" (familles, actif par défaut) et "Tendances" (tableau de corrélations). `_analyseTab()` est mode-aware (lit le filtre match pour choisir le bon jeu d'onglets et le bon préfixe d'id de panneau, `an-tab-*` vs `an-tab-saison-*`) ; un id de session invalide pour le mode courant (ancien id `resume`/`gardien`/`chat`, ou `tendances` en vue match) retombe sur le premier onglet du mode sans erreur JS ; changement de match réinitialise le drill-down ; transition Saison complète ↔ match re-sélectionné sans état invalide | ✅ 2026-09-09 (audit-complet-2026-09-09, v277, re-testé sur "J01 BILLERE-FENIX" — vue saison Tactique/Tendances + vue match Vue d'ensemble/Tactique/Notes & Outils, 0 erreur console). Historique des versions à 5 onglets (jusqu'à v271) conservé dans les rapports QA/E2E/audit datés référencés par les entrées de checklist antérieures |
| I2 | Page Notes (actions ATT/DEF, table GB) | v1, source Supabase depuis v241 (STORY-22), table GB corrigée en v246 | Filtre joueur/bilan → notes ATT+/-, DEF+/- et table GB affichées et cohérentes avec le filtre. **Comportement à connaître (non un bug)** : la section "Notes gardiens" (`#section-notes-gb`) reste masquée (`display:none`) tant que le filtre "Joueur" n'est pas positionné sur un gardien précis — une fois sélectionné, les 3 gardiens s'affichent (pas seulement celui choisi) | ✅ 2026-09-09 (audit-complet-2026-09-09, v277 — comportement filtre-dépendant re-confirmé sur Marius.C (joueur de champ, section GB masquée) puis Gabin.S (gardien, 3 gardiens affichés)) |
| I3 | Graphique évolution joueur (notegraph) | v1, filtre gardien corrigé en v246 | Depuis fiche joueur → bouton Graphique ouvre la progression du joueur sélectionné, pour un joueur de champ **et** pour un gardien | ✅ 2026-09-09 (audit-complet-2026-09-09, v277 — canvas Chart.js re-vérifié pour un joueur de champ, Antonin Vache) |
| I4 | Stats Gardien (fiche joueur GB) | v1 | Sélection d'un GB dans Joueurs → Fiche affiche arrêts/tirs, %, détail par match. Note : la page `page-gardiens` autonome n'est plus reliée à la navigation (aucun bouton ne l'appelle) — probable page orpheline comme `page-match` | ✅ 2026-09-09 (audit-complet-2026-09-09, v277 — fiche Gabin Saltel 11/54, 20%, détail par match cohérent) |
| I5b | Sous-navigation Joueurs sticky (Fiche/Notes/Graphique/Impact) | v1 ; `top` corrigé dynamiquement en v280 (même correctif que I1 ci-dessus) | `.joueurs-subnav` (`position:sticky`) doit rester visible, collée juste sous le header, quelle que soit la profondeur de scroll sur la fiche d'un joueur — même exigence que la barre de filtres Analyse (I1), même root-cause partagée (`top` figé/codé en dur ne suivant pas la vraie hauteur du header une fois le sélecteur de saison affiché) | ✅ 2026-09-15 (v280 — trouvé en creusant le rapport de Romain sur la barre de filtres Analyse (I1) : `.joueurs-subnav` avait un `top: 42px` codé en dur dans le CSS, avec exactement la même conséquence — sous-nav Fiche/Notes/Graphique/Impact entièrement invisible dès qu'on scrolle une fiche joueur. Corrigé par le même `ResizeObserver` partagé sur `.header` que I1, plutôt que deux correctifs séparés. Vérifié en conditions réelles sur la fiche d'Issa Seck : `top` passe de 42px à 80px, sous-nav visible après scroll, `docs/regression/screenshots/fix-joueurs-subnav-sticky.png`) |
| I5 | Page Impact (zones de tir ALG/face/ALD) | v1, seuils EFF_SEUILS, gère aussi un gardien depuis v247 (STORY-27, remplace le routage vers l'ancienne page-gardiens) ; en-tête scopé à la zone sélectionnée en v279 | 3 vues terrain se chargent, filtre bilan/joueur/résultat fonctionne, mode comparaison ne casse pas l'affichage. **Pour un gardien sélectionné** : source de données club adverse + finalite/gardien (pas resultat/joueur), libellés adaptatifs (Arrêts/Tirs subis), grille "Efficacité par zone" sans coloration par seuil trompeuse. **Quand une zone du sélecteur "ZONE" est cliquée** : l'en-tête BUTS/TIRS/EFFICACITÉ (`filteredAll` dans `updateImpactPage()`) doit refléter uniquement les tirs de cette zone — exactement les points visibles sur le nuage de tirs (`filtered`, même filtre `field_position`) et exactement le % affiché dans la cellule de la grille "ZONE" pour cette zone (`updateZoneEfficacite()`) — ces 3 nombres (en-tête, nuage, cellule zone) doivent toujours coïncider pour une même zone sélectionnée. Cliquer "✕ Tout voir" restaure l'en-tête sur le total toutes zones confondues | ✅ 2026-09-15 (v279 — bug signalé par Romain : en-tête affichait 9/18/50% (total toutes zones) alors même qu'une zone était sélectionnée et que le nuage de tirs ne montrait que 5 tirs dont 4 buts (80%) pour "6m ext G", donnant l'impression d'une incohérence de stats. Root-cause : `filteredAll` (calcul de l'en-tête) n'appliquait jamais `impactZoneFilter`, alors que le code de coloration de l'en-tête (`impactZoneFilter ? '#0f172a' : getEffColor(...)`) présupposait déjà un en-tête scopé à la zone — implémentation incomplète, pas juste un souci d'affichage. Corrigé en ajoutant le filtre de zone à `filteredAll`. Vérifié en conditions réelles sur "Issa.S" (joueur de champ, zone "6m ext G" : en-tête passe de 9/18/50% à 4/5/80%, cohérent avec le nuage et la cellule ZONE) ET sur "Enzo.D" (gardien, zone "6m central D" : en-tête passe à 0/1/0%, cohérent), `docs/regression/screenshots/fix-impact-zone-header-scoped.png`) |
| I16 | Page Impact mobile pour un gardien (Mode Lecture Joueur) | déjà correct depuis la refonte Vue Joueur Mobile (v96+), vérifié explicitement en v247 (STORY-28) | Un gardien connecté lui-même voit ses zones d'arrêt/encaissement sur l'onglet "Zones", cohérentes avec la vue staff | ✅ 2026-09-09 (audit-complet-2026-09-09, v277 — re-testé via impersonation sur Enzo Ditta, 17/46 arrêts 37%, cohérent) |
| I17 | Tooltip au survol des points Impact (canvas ALG/Central/ALD) | v1, isGardien lu dynamiquement depuis v248 (STORY-29, remplace un `false` en dur découvert lors du décommissionnement de `page-gardiens`) | Survol d'un point → marqueur correct dans l'infobulle : ●/vert pour un résultat positif (but marqué pour un joueur de champ, arrêt pour un gardien), ✕/rouge pour un résultat négatif (tir raté, but encaissé) | ✅ 2026-09-09 (audit-complet-2026-09-09, v277 — tooltip re-confirmé sur un joueur de champ ("● 6m central G Antonin.V"), 0 erreur console) |
| I6 | Familles d'enclenchement (camembert + matrice) | zone de churn intense v184→v221, déplacée dans l'onglet "Enclenchements" en v233, typographie des cards consolidée en v237 (STORY-16), tableau niveau 1 enrichi d'une colonne JF en v275 (STORY-44) | Camembert + matrice s'affichent sans doublon ni zone blanche, tooltip "i" correct. Cards `.enc-card-mini` : 3 niveaux typographiques max, drill-down par intention d'attaque fonctionnel sur clic. Tableau niveau 1 (`_buildEncDetailTable`, clic sur une famille) : colonnes Buts/PO/Ratés/PB/**JF**/Poss./Eff. — JF (jet franc subi) compté séparément des autres colonnes car ces lignes n'ont jamais de valeur "possession" (sous-événement, cf. `computeEncCoverage`), sinon toujours à 0. Clic sur une ligne d'intention → tableau niveau 2 par enclenchement (`_buildEncIntentionDetailTable`, sans colonne JF, inchangé), changement de famille réinitialise proprement l'intention sélectionnée | ✅ 2026-09-09 (audit-complet-2026-09-09, v277 — camembert + drill-down "Isoler" (colonne JF non nulle) + bascule Matrice 2×2 re-testés sur "J01 BILLERE-FENIX", `docs/qa/QA-44-drilldown-famille-enrichi.md`, `docs/e2e/E2E-44-drilldown-famille-enrichi.md`). Note antérieure conservée : sur données saison 2025-2026, "Intention attaque" est vide (100% non classifié) — confirmé normal par Romain, colonne introduite seulement à partir de la saison 2026-2027 |
| I7 | Gestion comptes joueurs (panneau) | v1, accès regroupé dans le menu "⚙ Outils" depuis v231 (STORY-12), modal centrée → panneau latéral en v238 (STORY-19), comptes sur Supabase Auth depuis v243 (STORY-24) | Création d'un compte joueur (nom + mot de passe) → utilisable immédiatement pour se connecter (cf. C2), via l'Edge Function `create-player-account`. Suppression d'un compte (🗑) → via l'Edge Function `delete-player-account`, compte définitivement inutilisable. Liste affiche nom/poste, plus aucun mot de passe visible ni stocké en clair. Accès via nav → "⚙ Outils" → "🔑 Comptes joueurs". Panneau glisse depuis la droite, overlay cliquable, Échap ferme + retour focus sur "Outils", Tab piégé dans le panneau | ⚠️ PARTIEL 2026-09-09 (audit-complet-2026-09-09, v277) — panneau ouvert et vérifié (formulaire, liste "Aucun compte joueur" cohérente avec l'absence de compte actif sur le projet), mais cycle création/suppression réel NON re-déclenché cette session par prudence (action d'écriture sur Supabase Auth). Dernier cycle complet réel : 2026-09-02 (audit-complet-2026-09-02, v256, sur Noah.O), code non modifié depuis |
| I13 | Note libre du coach par match (page Analyse) | v1, sur Supabase depuis v243 (STORY-24, remplace `fenix_coach_analyses`/localStorage) | Sélection d'un match → textarea "Ton Analyse" pré-rempli avec la note existante (le cas échéant) → "💾 Sauvegarder mon analyse" écrit dans `coach_analyses` (Supabase), visible depuis n'importe quel autre appareil après rechargement, sans réimport | ✅ 2026-09-09 (audit-complet-2026-09-09, v277 — rendu textarea vérifié (vide pour ce match, pas de note existante), 0 erreur console ; nouvelle sauvegarde non déclenchée par prudence — cycle complet écriture/lecture le plus récent : 2026-09-01, audit-complet-2026-09-01-soir, v248, code non modifié depuis) |
| I8 | Export PDF / PPT fiche joueur | résolu 2026-06-16 (CORS base64), couverture avec photo corps entier depuis v250 (STORY-31) | Génération PDF/PPT depuis fiche joueur → zones de tir (ALG/face/ALD) visibles dans le document exporté, photo corps entier sur la couverture si disponible | PDF : ⚠️ NON VÉRIFIABLE via Playwright — le bouton PDF appelle `window.print()` (page-joueurs.js:1170), boîte de dialogue native bloquant tout pilotage automatisé. Fonctionne normalement pour un utilisateur réel. **PowerPoint** : ⚠️ bouton présent et activé (re-vérifié 2026-09-09 sur la fiche Antonin Vache), non re-cliqué cette session — dernier export réel déclenché et téléchargé : 2026-09-01 (audit-complet-2026-09-01-soir2, v254, photo corps entier confirmée sur la couverture), code non modifié depuis |
| I9 | Menu "⚙ Outils" (nav) | v231 (STORY-12), "🔄 Migrer mes données locales" ajoutée en v242 (STORY-23), "🏷️ Familles tactiques" ajoutée en v244 (STORY-25), "📅 Bilans" ajoutée en v245 (STORY-26) | Clic sur "Outils" → menu avec "Comptes joueurs"/"Vue joueur"/"Familles tactiques"/"Bilans"/"Migrer mes données locales" (dans cet ordre) ; ouverture/fermeture (clic extérieur + Échap avec retour focus) ; navigation clavier complète (Tab/Entrée) ; les 3 boutons de page (Dashboard/Analyse/Joueurs) restent inchangés | ✅ 2026-09-09 (audit-complet-2026-09-09, v277 — menu à 5 entrées re-vérifié, Échap testé et ferme correctement) |
| I15 | Éditeur de bilans (panneau) | v245 (STORY-26) | Ajout/suppression d'un bilan (Saison/Nom/Journée fin) depuis le panneau "📅 Bilans" → écrit immédiatement dans `bilan` (Supabase), effet visible sur le filtre "Période" (page Notes/Joueurs/Impact) sans réimport, y compris pour un bilan ajouté hors ordre chronologique (`_rawBilanRows` toujours trié par journée de fin avant traitement) | ⚠️ PARTIEL 2026-09-09 (audit-complet-2026-09-09, v277) — panneau ouvert, 2 bilans existants affichés correctement (Bilan 1 J11, Bilan 2 J19) ; ajout/suppression réel NON re-déclenché cette session par prudence. Dernier test d'écriture réel : 2026-09-02 (audit-complet-2026-09-02, v256), code non modifié depuis |
| I14 | Éditeur de familles tactiques (panneau) | v244 (STORY-25) | Ajout/suppression d'une correspondance Intention attaque → Famille depuis le panneau "🏷️ Familles tactiques" → écrit immédiatement dans `famille_mapping` (Supabase), met à jour `FAMILLE_MAPPING` en mémoire, effet visible immédiatement sur les cards/camembert de la page Analyse sans rechargement ni réimport. Bandeau "configuration initiale" affiché tant qu'aucune modification n'a été faite | ⚠️ PARTIEL 2026-09-09 (audit-complet-2026-09-09, v277) — panneau ouvert, 17 correspondances existantes affichées correctement, bandeau "configuration initiale" toujours présent ; ajout/suppression réel NON re-déclenché cette session par prudence. Dernier test d'écriture réel : 2026-09-02 (audit-complet-2026-09-02, v256), code non modifié depuis |
| I11 | Panneau "Vue joueur" (preview staff) | v1, modal centrée → panneau latéral en v238 (STORY-19) | Sélection d'un joueur dans le panneau → "Voir sa vue →" bascule réellement en mode joueur avec le bon nom, bouton "← Retour staff" fonctionnel. Panneau glisse depuis la droite, overlay cliquable, Échap ferme + retour focus, Tab piégé | ✅ 2026-09-09 (audit-complet-2026-09-09, v277 — cycle complet re-testé sur un gardien réel, Enzo Ditta : bascule + "← Staff" fonctionnels, 0 erreur console) |
| I12 | Migration locale → Supabase (prompt F5 + menu Outils) | v242 (STORY-23) | Au premier login staff avec données locales (`fenix_coach_analyses`/`fenix_player_accounts`/`enc_famille_custom`) non migrées → prompt avec décompte réel ; "Migrer maintenant" upsert notes/familles + appelle l'Edge Function par compte joueur (statut affiché par compte) ; "Annuler" ne pose pas de flag (reprompt au chargement suivant) ; entrée "🔄 Migrer mes données locales" toujours disponible dans "⚙ Outils" ; jamais affiché en session joueur | ✅ 2026-09-09 (audit-complet-2026-09-09, v277 — état "Aucune donnée locale à migrer sur cet appareil" re-confirmé, 0 erreur console) |
| I18 | Photos joueurs — avatar portrait (fiche staff + mode joueur mobile) | v250 (STORY-30), préchargement ajouté en v254 | `.jp-avatar`/`.pmf-avatar` affichent la photo portrait (`getPlayerPhoto(nom,'portrait')`) si disponible pour le joueur sélectionné, sinon repli sur les initiales sans erreur. Les 36 fichiers (`assets/photos/`) sont préchargés dès le boot du script (`js/player-photos.js`), avant même la connexion | ✅ 2026-09-09 (audit-complet-2026-09-09, v277 — vérifié staff (Antonin Vache, Marius Canitrot, Gabin Saltel) et mode joueur mobile (Enzo Ditta), fallback initiales confirmé sur le terrain (RL/YC/ZD)) |
| I19 | Photos joueurs — terrain avec photos + bascule directe vers la photo corps entier | v250 (STORY-32), terrain photo + espacement gardien en v251-253, préchargement en v254 | Les ronds du terrain (page Joueurs) affichent la photo portrait si disponible (sans marge visible, ~25% plus grands que l'ancien format initiales), anneau de couleur d'efficacité toujours visible en bordure. Positions GB espacées sans chevauchement. Clic sur un rond → sélection ET bascule directe en photo corps entier grand format (pas de clic supplémentaire), sans latence visible (photos préchargées). Bouton "↩ Terrain" pour revenir ; un retour manuel au terrain n'est pas annulé par un simple changement de filtre (seule une nouvelle sélection redéclenche la bascule) | ✅ 2026-09-09 (audit-complet-2026-09-09, v277 — clic direct re-testé sur Antonin Vache et Gabin Saltel, bascule immédiate en photo corps entier confirmée, retour terrain fonctionnel) |
| I20 | Photos joueurs — photo corps entier sur la couverture d'export PDF/PPT | v250 (STORY-31) | La page de couverture de l'export (`pdf-slide-cover`) affiche la photo corps entier du joueur en bas-droite si disponible, sans chevauchement avec le texte ; couverture identique à l'existant si le joueur n'a pas de photo | ⚠️ NON RE-DÉCLENCHÉ cette session (export non re-cliqué, cf. I8) — dernier test réel : 2026-09-02 (audit-complet-2026-09-02, v256, photo confirmée présente dans le DOM avant capture), code non modifié depuis |
| I21 | Import "Articulation défensive" (colonnes `articulation_def`/`p1`-`p6`) | v257 (STORY-33) | Réimport Excel avec les 7 nouvelles colonnes → valeurs synchronisées jusqu'à `match_data` (Supabase), résilient à un ancien format sans ces colonnes (colonnes absentes proprement, aucune erreur) | ⚠️ NON RE-TESTÉ cette session (réimport Excel évité, action destructive sur données prod, cf. C3) — dernière vérification réelle : 2026-09-02 (v257, 166/540 lignes taguées vérifiées par requête REST directe sur Supabase, `docs/e2e/E2E-18-import-articulation-defensive.md`), code non modifié depuis. Aucun affichage exploitant cette donnée pour l'instant (STORY-34/35 à venir) |
| I29 | Import "Articulation offensive" (colonnes `articulation_att`/`att_alg`/`att_arg`/`att_dc`/`att_ard`/`att_ald`/`att_pvt`) | v282 (STORY-47) | Réimport Excel avec les 7 nouvelles colonnes (en-têtes `ARTICULATION ATT`/`ALG`/`ARG`/`DC`/`ARD`/`ALD`/`PVT`) → valeurs synchronisées jusqu'à `match_data` (Supabase) **et** correctement reconstruites dans `DATA` (positions 29-35, `MATCH_DATA_COLUMN_ORDER`/`rowToPositionalArray`), résilient à un ancien format sans ces colonnes (colonnes absentes proprement, aucune erreur). Migration SQL manuelle préalable requise (7 `alter table` sur `match_data`, cf. story) — sans elle l'import échoue silencieusement pour ces colonnes, pas d'erreur bloquante sur le reste | ✅ 2026-09-16 (v282/STORY-47, réimport réel du fichier de la saison en cours par Romain sur la prod, `docs/qa/QA-47-import-articulation-attaque.md` — 120/350 lignes taguées vérifiées par requête REST directe sur Supabase, valeurs identiques à l'Excel source, et round-trip `DATA`/`COLS` vérifié par script Node reproduisant le code réel sur 3 lignes réelles, 0 divergence. **Incident rencontré et documenté** : le premier réimport (avant rechargement forcé du navigateur) n'a rien peuplé — onglet resté sur l'ancien JS, pas un bug du code). Aucun affichage exploitant cette donnée pour l'instant (STORY-48 à venir) |
| I22 | Mode "Articulation" côté Défense — flux unique (largeur → classement → terrain) | v268 (STORY-34/36/37/38 ; historique v258→v267 dans les rapports datés, UI de ces versions remplacée par STORY-38, pas cumulée) ; **bouton devenu cliquable aussi en mode Attaque en v284/STORY-48, cf. I30 — comportement décrit ici (dispositif, `_articTauxDefense` inversée, état `_artic*`) inchangé au pixel près** | Bouton "🎯 Articulation" à côté de Vue générale/Matrice 2×2. Bandeau `.artic-control-bar` réduit à 2 choix : DISPOSITIF (0-6/1-5) et LARGEUR (À6/À4/À2, `ARTIC_BLOCKS`). Terrain (identification des postes, aucune évaluation individuelle) : clic sur un rond ouvre `.artic-poste-editor` sous le terrain (joueurs déjà observés cliquables + `<select>` avec "Auto" en tête, `_toggleArticPosteEditor`/`_setArticManualJoueur`). Sous l'éditeur, `.artic-summary` affiche pour la largeur active le % de **réussite défensive** (`_articTauxDefense`, plus haut = mieux ; `_articEffClass`/ancienne efficacité attaque adverse non appelée, ⚠️ ne pas la réintroduire) + détail But/Tir raté/PB/PO/Jet franc (`_articBlockDetail`, `COLS.finalite`, sous-catégories de tir regroupées sous "Tir raté"). Colonne de droite `.artic-listing-col` : classement de la largeur active groupé Fiable (≥5 séq.)/Échantillon faible (`_articRankedCombos`), lignes cliquables (`_setArticManualCombo`) qui placent toute la composition d'un coup. Postes sur la courbe de l'arc 6m via `ARTIC_LAYOUTS`/`_articArcY()`. Résolution des noms via `matchPlayerName()` | ✅ 2026-09-09 (audit-complet-2026-09-09, v277 — bascule Défense→Articulation re-testée sur "J01 BILLERE-FENIX", Dispositif/Largeur + terrain avec postes numérotés rendus correctement, 0 erreur console). ⚠️ **NON RE-CLIQUÉ en v284** (MCP Playwright indisponible, `CONNECT_TIMEOUT`) — non-régression déduite de `git diff` (zéro ligne des fonctions défense `computeArticulationStats`→`_toggleArticPosteEditor` modifiée par STORY-48, seules 3 lignes hors de ce bloc touchées, cf. `docs/code-review/STORY-48.md`), pas d'un vrai clic navigateur — à re-tester par Romain en priorité |
| I26 | Timeline — courbe d'écart, grille 10 min, bascules sur la courbe | v274 (STORY-43, remplace les 2 courbes absolues FENIX/Adversaire + l'ancien overlay `drawMomentumOverlay`) ; infobulle bridée au viewport en v278 | `drawTimeline()` trace l'écart FENIX-Adversaire en aire dégradée (vert = FENIX devant, rouge = adversaire devant), grille verticale toutes les 10 min (0/10/.../60), losange + ligne pointillée sur la courbe pour chaque bascule (`_bascules`, `b.apres`), cercle + numéro pour chaque moment clé (`_momentsCles`), ligne de mi-temps si période fiable. `#timeline-scores` (MT1/Final) inchangé. Tooltip au survol (`initTimelineTooltip`) et clic pour dérouler les séquences (`showMCSequences`) fonctionnels sur les 3 types de marqueurs (mc/bascule/score) via `window._timelineHitAreas`. **L'infobulle (`#timeline-tooltip`, `position:fixed`) doit rester entièrement visible** : positionnée par défaut à `clientX+14`, elle se rabat à gauche du curseur si elle déborderait à droite, et sa position verticale est plafonnée pour ne jamais déborder en bas — un marqueur proche du bord droit/bas de l'écran ne doit jamais couper l'infobulle. DPR scaling conservé (net sur écran Retina). Redessine correctement après changement de match/filtre/redimensionnement | ✅ 2026-09-14 (v278 — bug de débordement d'infobulle en bord d'écran signalé par Romain sur la Matrice 2×2, même correctif appliqué par prudence ici car le code partage le même schéma non bridé ; vérifié en conditions réelles : marqueur "score" au point le plus à droite de la courbe, viewport réduit à 1000px pour forcer le cas limite, infobulle confirmée entièrement dans le viewport après le correctif) |
| I27 | Matrice 2×2 (fréquence × efficacité par famille) | v276 (STORY-45, refonte visuelle uniquement — positions des bulles inchangées) ; infobulle bridée au viewport en v278 | `_drawEncMatrix()` (à ne pas confondre avec `_drawEncRadar()`, dead code jamais appelé, laissé tel quel) — matrice agrandie (plafond 430→500px), 4 quadrants teintés plus francs avec libellé coloré ("Sous-utilisé"/"Exploiter ⭐"/"Abandonner"/"Corriger ⚠"), bulles avec ombre portée, nom de famille + eff% toujours affichés à côté de chaque bulle (anti-collision entre libellés avec marge 5px, centre du texte bridé aux bords du canvas pour ne jamais déborder hors-champ — ce bridage concerne le libellé permanent dessiné sur le canvas, pas l'infobulle de survol). Survol d'une bulle → agrandissement (×1.25) + libellé en gras coloré (`_encMatrixHoverFamille`, redessin ciblé dans `_initEncMatrixHover`) **+ infobulle HTML (`#enc-matrix-tooltip`, `position:fixed`) bridée au viewport** : se rabat à gauche du curseur si elle déborderait à droite (bug réel signalé — la famille la plus utilisée est toujours tracée au bord droit du graphique, donc sa bulle est la plus susceptible d'être proche du bord droit de l'écran), position verticale plafonnée pour ne jamais déborder en bas. Fonctionne identique en Attaque/Défense et en vue saison | ✅ 2026-09-14 (v278 — bug de débordement d'infobulle hors écran signalé par Romain par capture d'écran, sur la bulle "Jeu Rapide" — famille avec le plus de possessions, donc positionnée à l'extrême droite du graphique par construction de l'axe X. Root-cause : `#enc-matrix-tooltip` était positionnée `clientX+14` sans jamais vérifier le débordement à droite ni en bas. Corrigé en calculant la largeur/hauteur réelles de l'infobulle après `innerHTML` et en la rabattant à gauche du curseur si elle déborderait, avec plafond vertical. Vérifié en conditions réelles : viewport réduit à 1000px, hover sur la bulle "Jeu Rapide" (bulle la plus à droite), infobulle confirmée entièrement dans le viewport après le correctif, `docs/regression/screenshots/fix-matrice-tooltip-overflow.png`) |
| I25 | Terrain & Comparatif — accordéon replié + mode Météo | v273 (STORY-41) | Bloc terrain/comparatif (`#match-block-section`) replié par défaut (`.terrain-accordion-body` sans classe `.open`) à chaque ouverture d'un match, jamais mémorisé ouvert. Clic sur l'en-tête déplie/replie (transition `max-height` ~0.2s), chevron pivote sans retourner le texte "Déplier"/"Replier" (symbole et libellé dans des éléments séparés). Une fois déplié : toggle `Nuage de tirs`/`🌡️ Météo` (`_terrainMode`, `setTerrainMode()`) — Météo peint un dégradé rouge→vert par zone (grille 6×4, ratio but/(but+tir raté), `drawTerrainMeteo()`), cellule à échantillon faible (<5 tirs) atténuée, aucun % affiché sur le terrain. Le mode choisi survit à un changement de filtre Club/Résultat/GE ; `drawTerrain()` redessine correctement à l'ouverture de l'accordéon même si le canvas était à 0×0 pendant qu'il était fermé | ✅ 2026-09-09 (audit-complet-2026-09-09, v277 — replié par défaut confirmé à l'ouverture du match, déplié/replié + bascule Nuage de tirs↔Météo re-testés sur "J01 BILLERE-FENIX", `docs/qa/QA-41-accordeon-terrain-meteo.md`, `docs/e2e/E2E-41-accordeon-terrain-meteo.md`) |
| I24 | Bloc "Essentiel" page Analyse (match + saison) | v271 (STORY-39) | Toujours visible avant tout onglet, avant le bloc terrain, sans scroller. **Vue match** : reprend le HTML de `generateResume3Points()` (résultat + top 3 constats), écrit dans `#essentiel-match`, classe `essentiel {victoire\|defaite\|nul}` posée sur le conteneur pour la bordure gauche colorée. **Vue saison** : `#essentiel-saison` (`_renderEssentielSaison()`, appelée depuis `generateSeasonCorrelations()`) affiche "N matchs analysés" + décompte V/D/N + les 1-2 signaux forts les plus importants (déjà calculés pour le tableau de corrélations, triés par écart décroissant) ; message de repli explicite si <3 matchs ou <2 types de résultat, jamais un bloc vide. `#essentiel-match`/`#essentiel-saison` basculés en miroir exact de `#analyse-content`/`#analyse-empty` par `updateAnalysePage()`. L'onglet "Résumé" ne contient plus ce résumé en double (carte `.ia-card` retirée) | ✅ 2026-09-09 (audit-complet-2026-09-09, v277 — vue saison (4 matchs, 1V/3D/0N + signaux forts) et vue match (défaite 20-29, bordure rouge, 3 constats) re-vérifiées) |
| I23 | Indicateurs clés page Analyse — Rythme du match + Supériorités/infériorités numériques | v270 (STORY-42) ; prise en compte des lignes "Pen" en v283 | Carte "Rythme du match" : une seule valeur (possessions FENIX), jauge à 4 paliers (Faible 50-53/Normal 53-56/Élevé 56-60/Très élevé 60+), pas de comparaison FENIX/Adversaire. Carte "Supériorités/infériorités numériques" (`computeSuperiorites()`, `js/page-analyse.js`) : score agrégé exact ("FENIX X — Y Adversaire") sur toutes les séquences où `phase_att` contient `+` ou `-` (peu importe le club), plus efficacité en possessions de chaque équipe, sans découpage mi-temps ; ⓘ au survol explique la méthode ; match sans `phase_att` renseigné → "Non disponible pour ce match", jamais "0 — 0" silencieux. **Lignes "Pen" (7m)** : `phase_att` vaut littéralement "Pen" sur ces lignes (jamais "+"/"-" directement, comme "Jet franc" n'a jamais de `possession`) — un Pen prolonge toujours la séquence en cours, jamais une possession isolée, donc `computeSuperiorites()` remonte chronologiquement (tri explicite par `Position`/`parseTimecode`, ne suppose jamais l'ordre d'arrivée de `matchData`) jusqu'à la ligne non-Pen précédente et hérite de son signe +/- pour décider si ce Pen doit être compté. Le bloc "Résumé du match" (candidat Supériorités de `generateResume3Points`) ET la réponse Chat IA "Supériorités numériques" (`generateChatResponse`) utilisent la même fonction — les 3 endroits doivent toujours donner un bilan cohérent entre eux pour un même match | ✅ 2026-09-15 (v283 — bug signalé par Romain : les buts sur pénalty (7m) marqués pendant une séquence de supériorité/infériorité n'étaient jamais comptés, car `phase_att="Pen"` sur ces lignes ne contient ni "+" ni "-" et était donc silencieusement ignoré par le filtre. Vérifié avec un script Node reproduisant exactement l'ancienne et la nouvelle logique sur les 2 matchs réels de la saison en cours : "J01 BILLERE-FENIX" passe de FENIX 6/13-Adversaire 5/8 à **FENIX 9/16-Adversaire 6/9** (3 buts sur pénalty récupérés, 2 en infériorité et 1 en supériorité pour FENIX, 1 en supériorité pour l'adversaire) ; "J02 FENIX-LA CRAU" passe de 5/6 à **6/7** côté Adversaire. Écart réel et non négligeable sur les 2 matchs disponibles, pas un cas isolé) |
| I30 | Mode "Articulation" côté Attaque (miroir offensif de I22) | v284 (STORY-48) ; règle de comptage `_articAttCounts` affinée en v285 (même résultat numérique, raisonnement corrigé après retour de Romain) ; **bug réel trouvé et corrigé en v286** — basculer Attaque↔Défense ou changer de match pendant que le mode Articulation est actif laissait `#enc-articulation-wrap` complètement vide (`renderEncFamillesSection` reconstruit tout le HTML sans jamais rappeler la fonction de dessin, seul `_setEncGraphMode` le faisait) ; ce bug touchait **aussi le mode Défense** depuis toujours (STORY-34), jamais détecté car jamais testé dans cet ordre précis (Articulation actif → puis bascule/changement de match) avant Playwright réel sur STORY-48 | Bouton "🎯 Articulation" cliquable en mode ⚡ Attaque (n'affichait rien avant cette story, cf. I22). Demi-terrain 6 postes (ALG/ARG/DC/ARD/ALD/PVT, `ARTIC_ATT_LAYOUT` — PVT/ALG/ALD sur l'arc des 6m, ARG/DC/ARD sur l'arc des 9m). Pas de bandeau DISPOSITIF (n'existe pas côté attaque) — seule la ligne LARGEUR reste, 2 choix : "6 complet"/"Base arrière" (`ARTIC_ATT_BLOCKS`). `.artic-summary` affiche le % de **réussite offensive** en sens direct (`_articAttEffClass`, plus haut = mieux, **pas d'inversion** contrairement à la défense) + même détail But/Tir raté/PB/PO/Jet franc. Classement `.artic-listing-col` identique en structure (Fiable ≥5 séq./Échantillon faible, tri décroissant sur `eff`). État `window._articAtt*` strictement séparé de `window._artic*` (défense) — bascule Attaque↔Défense en mode Articulation doit conserver poste ouvert/override manuel/groupement actif de chaque côté indépendamment. **Filtre de comptage** (`_articAttCounts`, règle confirmée par Romain 2026-09-16) : un tir (`But`/`Tir raté`) compte toujours dès que `articulation_att` est renseigné, **la possession n'entre pas en jeu pour un % de réussite au tir** ; un sous-événement (`PB`/`PO`/`Jet franc`/`2' obt`) reste filtré par le tag `Possession` (sert à dédoublonner une séquence multi-lignes). Sur données réelles, 4 des 28 tirs ratés FENIX tagués `articulation_att` n'ont pas le tag `Possession` (contre 0/31 buts) — vérifiés non-doublons ET vérifié qu'aucun n'est un Pen (les 8 lignes FENIX `phase_att="Pen"` de la saison n'ont jamais `articulation_att` rempli, donc Pen ne peut structurellement jamais entrer dans ce calcul) — ⚠️ si une future anomalie de comptage apparaît, vérifier `_articAttCounts` avant de suspecter autre chose | ⚠️ **NON VÉRIFIÉ PAR CLIC RÉEL** (MCP Playwright indisponible, `CONNECT_TIMEOUT`, tout ce cycle `/verifie`) — calculs vérifiés par script Node reproduisant fidèlement les fonctions sur données réelles (saison entière 88 séq./31 buts/28 tirs/53%, J01 seul 42 séq./11 buts/13 tirs/46%, classement Base arrière cohérent), câblage des 3 points d'intégration et absence de classe CSS manquante vérifiés par lecture de code (`docs/code-review/STORY-48.md`). **Clic-terrain, panneau d'édition, clic sur une ligne de classement, et bascule Attaque↔Défense avec conservation d'état (critère 7) restent à valider par Romain en conditions réelles avant utilisation en réunion tactique** |
| I28 | Chat IA — questions types suggérées (chips) | v277 (STORY-46, purement additif au-dessus du chat existant, `generateChatResponse()` non modifiée) | 5 boutons `.chat-chip` affichés au-dessus du fil de conversation, dans l'onglet "Notes & Outils". Chaque clic (`_sendChatSuggestion(text)`) déclenche la même réponse qu'une saisie manuelle identique via `sendChatMessage()` réutilisée telle quelle (pas de logique dupliquée) — champ vidé après clic, historique accumulé normalement sur des clics consécutifs. Les 5 libellés doivent chacun tomber sur une branche pertinente et distincte de `generateChatResponse()` (pas la réponse générique) : vérifié empiriquement avant figement plutôt que recopié des exemples illustratifs de la story (qui, eux, ne fonctionnent pas). **Comportement pré-existant à connaître (non un bug de cette story)** : un redimensionnement de la fenêtre navigateur réinitialise tout le chat (et le brouillon de note coach non sauvegardé) via `window.addEventListener('resize', ...)` → `refreshPage()` (`FENIX-HANDBALL-CF-SUIVI.html:3492`) qui rappelle `updateAnalysePage()` — touche identiquement une conversation démarrée par saisie manuelle, aucun lien avec les chips | ✅ 2026-09-09 (audit-complet-2026-09-09, v277 — chip "Comment on a marqué ?" re-testée sur "J01 BILLERE-FENIX", réponse pertinente affichée, 0 erreur console ; couverture complète des 5 chips déjà faite en QA/E2E-46, `docs/qa/QA-46-chat-ia-questions-types.md`, `docs/e2e/E2E-46-chat-ia-questions-types.md`) |

## Secondaire (hors périmètre par défaut — non testé sauf demande explicite)

| # | Feature | Note |
|---|---------|------|
| S1 | Page Match (`page-match`) | Désactivée dans le HTML (`display:none !important`) — dead page, pas de nav vers elle |
| S2 | Chat IA rule-based (page Analyse) | Fonctionnalité secondaire, backlog prévoit migration vers API Claude |
| S3 | Détails cosmétiques (couleurs, tailles canvas) | Nombreux commits v190-v221 sont purement visuels |

---

## Méthode de mise à jour
- Après chaque story livrée + validée QA (et E2E Tester si convoqué) : ajouter/mettre à jour la ligne correspondante.
- Avant chaque mise en production : parcourir en priorité Critique puis Important, cibler ce qui est plausible à risque avec la version en cours.
- Réutiliser les parcours déjà exécutés en conditions réelles (`docs/e2e/E2E-*.md`) plutôt que de repartir de zéro.
