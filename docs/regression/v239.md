# Régression — v239 (STORY-20 : Setup Supabase + schéma de base)

**Agent :** Regression Guardian
**Date :** 2026-08-31
**Changement :** `FENIX-HANDBALL-CF-SUIVI.html` (2 lignes : script CDN Supabase + référence à `js/supabase-client.js`), nouveau `js/supabase-client.js` (non appelé par le reste de l'app à ce stade). Infrastructure Supabase créée hors repo (tables, RLS, Edge Function).

---

## Analyse de risque

Changement **additif et inerte** — aucune fonction existante n'a été modifiée, aucun appel à Supabase n'est encore déclenché par le code applicatif (import, boot, panneaux). Le seul risque possible est que l'ajout des 2 nouveaux `<script>` casse le chargement de la page (script manquant, erreur de syntaxe, conflit de nommage global). Testé directement plutôt que supposé sans risque.

## Vérification

- Chargement de l'app en conditions réelles (navigateur, session fraîche) : aucune erreur console, écran d'import affiché normalement — comportement identique à avant ce changement.
- `window.supabase`, `supabaseClient`, `fetchAll`, `replaceTable`, `upsertRows` tous correctement définis et fonctionnels, vérifiés depuis la page elle-même (pas seulement supposés depuis le code source).
- Aucune des features de la checklist (C1-C7, I1-I11) n'est concernée par ce changement — pas de re-test ciblé nécessaire au-delà du smoke test général de chargement, puisqu'aucune de leurs fonctions n'a été touchée.

## Nouvelle ligne de checklist

Pas encore ajoutée : l'infrastructure Supabase n'est pas encore une feature utilisateur observable (rien n'en dépend dans l'app à ce stade). Une ligne "Import/lecture Supabase" sera ajoutée à la checklist une fois STORY-21/22 livrées et le flux réellement branché et testable de bout en bout côté utilisateur.

## Verdict

**RAS** — aucune régression détectée sur l'existant. Checklist inchangée (rien à mettre à jour, pas de feature utilisateur nouvelle ou modifiée à ce stade).
