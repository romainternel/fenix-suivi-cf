# Régression — v272 (STORY-40 : regroupement des onglets 5→3 en vue match, structure commune en vue saison)

**Agent :** Regression Guardian
**Date :** 2026-09-08
**Changement :** `FENIX-HANDBALL-CF-SUIVI.html` (5 panneaux `#an-tab-*` regroupés en 3 en vue match ; nouveau système d'onglets à 2 panneaux `#an-tab-saison-*` ajouté en vue saison), `js/page-analyse.js` (`AN_TABS_MATCH`/`AN_TABS_SAISON` remplacent `AN_TABS`, `_analyseTab()` mode-aware, `updateAnalysePage()` appelle `_analyseTab()` dans les deux branches), `CLAUDE.md` (v271 → v272)

---

## Analyse de risque

Zone modifiée = le mécanisme central de navigation interne de la page Analyse, utilisé par 8 sections de contenu différentes (Indicateurs, Timeline, Moments clés, Bascules, Familles, Gardien, Coach, Chat). Risque principal : qu'un contenu déplacé perde son déclenchement de rendu (notamment les canvas, dont le dimensionnement dépend de la visibilité du conteneur au moment du dessin) en changeant de nom/conteneur d'onglet.

## Vérification

- **Aucune référence résiduelle** aux anciens ids d'onglet (`an-tab-resume`, `an-tab-timeline`, `an-tab-enclenchements`, `an-tab-gardien`, `an-tab-chat`) ni à l'ancienne constante `AN_TABS` — recherche exhaustive sur tout le projet, uniquement des mentions dans des rapports datés (attendu, historique).
- **Canvas Timeline** (renommé de l'onglet "timeline" vers "overview") : re-vérifié en conditions réelles sur 2 matchs — le graphique se dessine correctement dès l'ouverture par défaut de "Vue d'ensemble" (premier onglet).
- **Camembert/matrice familles** (renommé de "enclenchements" vers "tactique", désormais partagé match ET saison) : re-vérifié sur les deux contextes — se dessine correctement au premier clic sur "Tactique", en vue match ET en vue saison (cas nouveau introduit par cette story, potentiellement le plus à risque puisque la vue saison n'avait jamais eu ce mécanisme de redessin-à-l'ouverture avant).
- **sessionStorage `an_active_tab` partagé entre 2 jeux d'onglets** (risque spécifique à cette story, cf. Risk R2 du cycle de réorganisation) : testé avec un ancien id (`resume`) et avec un id valide dans l'autre mode (`tendances` en vue match) — retombe systématiquement sur le premier onglet du mode actif, aucune erreur JS.
- **Non-régression Coach/Chat** : Coach re-vérifié visuellement (placeholder propre après correction de l'incident de test, cf. ci-dessous) ; Chat IA re-testé avec une vraie question, réponse cohérente.
- **Non-régression Essentiel (I24, v271) et Indicateurs (I23, v270)** : les deux continuent de fonctionner normalement à l'intérieur de la nouvelle structure d'onglets, testés sur les mêmes matchs.
- **Navigation générale** : 0 erreur console sur l'ensemble des parcours testés (Developer, QA, E2E, 3 matchs différents + vue saison).

## Incident de test signalé (pas une régression de code)

Le QA a déclenché une écriture réelle sur Supabase production (`coach_analyses`) en testant le bouton de sauvegarde Coach — corrigée immédiatement (suppression de la ligne test, état restauré et vérifié par requête REST indépendante). Ce risque existe pour toute story future testant une action d'écriture en local, puisque le projet n'a pas d'environnement de test séparé (`SUPABASE_URL` pointe toujours vers la production, cf. `js/supabase-client.js`) — pas spécifique à STORY-40, mais à garder en tête systématiquement pour les prochains cycles de contrôle.

## Verdict

**RAS** — aucune régression de code détectée. Checklist mise à jour (I1 et I10 réécrites pour refléter l'état actuel plutôt que d'accumuler l'historique des 5 onglets, cf. pattern déjà appliqué à I22).
