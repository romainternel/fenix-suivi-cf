# Régression — v247 (STORY-27 + STORY-28 : Page Impact pour un gardien)

**Agent :** Regression Guardian
**Date :** 2026-09-01
**Changement :** `FENIX-HANDBALL-CF-SUIVI.html` (`openImpactForSelected()`, `updateImpactPage()`, `drawImpactCanvas()`, `updateZoneEfficacite()`, `populateFilters()`, nouvelle constante `POSTE_LABELS`), `js/page-joueurs.js` (`printFicheJoueur()` réutilise `POSTE_LABELS`)

---

## Analyse de risque

Changement plus profond que les corrections précédentes du jour (v246) : `updateImpactPage()` est entièrement réécrite (branches `isGB`), `openImpactForSelected()` fusionne deux chemins auparavant séparés, `populateFilters()` change la construction de 2 listes de filtres partagées entre pages (Impact et Notes). Risque de régression réel sur la fonctionnalité Critique/Important I5 (Impact) — traité en priorité avec un test de non-régression exact, pas approximatif.

## Vérification

- **I5 (Page Impact, joueur de champ)** : Antonin Vache re-testé, chiffres strictement identiques (2/4, 50%) à toutes les vérifications précédentes de cette session, mêmes points sur les mêmes vues terrain.
- **I5 (Page Impact, gardien)** : nouveau comportement, testé explicitement sur les 3 gardiens réels, cohérent avec 3 autres sources indépendantes (fiche joueur, table GB, onglet Gardien Analyse).
- **I2 (Page Notes)** : `populateFilters()` modifiée affecte aussi `filter-note-joueur` (bénéfice collatéral documenté) — non re-testé en détail spécifiquement dans ce cycle, mais le changement ne fait qu'ajouter des options à une liste déjà fonctionnelle, risque de régression jugé très faible (pas de suppression, pas de changement de la logique de filtrage elle-même côté Notes).
- **I16 (nouveau, mode mobile)** : testé explicitement, aucun changement de code sur ce chemin, aucun risque de régression possible par construction.
- **I7 (Comptes joueurs)** : re-testé incidemment (création/suppression du compte de test pour STORY-28) — fonctionne comme avant.

## Verdict

**RAS** — aucune régression détectée. Donnée réelle de production (`player_profiles`) laissée vide après nettoyage du compte de test, vérifié via l'API REST.
