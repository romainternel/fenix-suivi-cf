# Régression — v236 (STORY-18 : Passe visuelle mode joueur mobile)

**Agent :** Regression Guardian
**Date :** 2026-08-28
**Changement :** `css/style.css` (`.pm-tab-btn`, `.pmf-card`, `.pmf-kpi-box`) + `js/player-mode.js` (2 lignes, couleurs d'en-tête `makeSection()` uniquement). Aucune donnée, aucune logique de calcul, matching ou navigation touchée.

---

## Analyse de risque

Zone qualifiée "sensible" par la story elle-même (plusieurs correctifs mobiles la même semaine : canvas Impact, header sticky, matching noms, temps de jeu, badge signature). Risque principal : qu'un changement CSS pur bascule silencieusement l'un de ces correctifs récents. Traité par une vérification empirique explicite des 4 sous-critères de non-régression à 375px, plutôt qu'une inspection de code seule.

## Vérification

- C6 Mode Lecture Joueur (Ma Fiche) : re-testé en conditions réelles (login réel + preview réelle, 3 joueurs différents sur 2 sessions Playwright distinctes) — RAS.
- Canvas Impact pleine largeur en premier : confirmé visuellement (zone CENTRAL en premier, EXT G/EXT D en dessous).
- Header sticky Stats Match : confirmé non masqué au chargement ET après scroll (`window.scrollTo(0,400)`).
- État vide "Données non disponibles" : confirmé avec une session joueur réelle et le cache local vidé.
- Badge signature sans parenthèse : confirmé après réimport réel sur un joueur à signature forte (Isaac, "But").
- `pmTab()` (mécanique des onglets) : non modifié, navigation entre les 3 onglets testée fonctionnelle sur les 2 sessions.

## Verdict

**RAS** — aucune régression détectée. Checklist mise à jour (C6).
