# Régression — v248 (STORY-29 : décommissionnement de `page-gardiens`)

**Agent :** Regression Guardian
**Date :** 2026-09-01
**Changement :** `FENIX-HANDBALL-CF-SUIVI.html` — suppression de markup + 4 fonctions + 2 variables globales (`page-gardiens` et son sous-système), correction du bug tooltip (`_impactIsGB`)

---

## Analyse de risque

Suppression de code, par nature un risque de régression si une référence oubliée subsiste (fonction/élément DOM appelé mais introuvable → erreur JS silencieuse ou visible selon le contexte). Recherche exhaustive élargie au-delà de la liste initiale de l'Architecture (voir Code Review STORY-29) — 4 références supplémentaires trouvées et nettoyées avant validation.

## Vérification

- **I5 (Page Impact, gardien et joueur de champ)** : re-testé après suppression — comportement identique à STORY-27 (v247).
- **I17 (nouveau, tooltip)** : bug réel découvert et corrigé pendant ce nettoyage, testé explicitement dans les deux sens.
- **Navigation générale** (Dashboard/Analyse/Joueurs, changement de saison) : 0 erreur console.
- **`refreshPage()`** (switch générique) : le retrait du `case 'gardiens':` ne perturbe pas les autres cas du switch (testé par navigation réelle entre toutes les pages).

## Verdict

**RAS** — aucune régression détectée. Un bug préexistant (tooltip) a été trouvé et corrigé en marge de ce nettoyage, sans lien avec la suppression elle-même mais découvert grâce à elle.
