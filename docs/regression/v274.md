# Régression — v274 (STORY-43 : Timeline — refonte visuelle et bascules sur la courbe)

**Agent :** Regression Guardian
**Date :** 2026-09-09
**Changement :** `js/page-analyse.js` (`drawTimeline()` réécrite, `drawMomentumOverlay()` supprimée), `CLAUDE.md` (v273 → v274)

---

## Analyse de risque

Zone modifiée = le rendu d'un canvas interactif (tooltip + clic vers un panneau de séquences), avec un changement de type de graphique (2 courbes absolues → 1 aire d'écart). Risque principal : casser le système d'interaction (`window._timelineHitAreas`) en changeant la géométrie sans préserver la forme des objets qu'il consomme.

## Vérification

- **Système d'interaction préservé** : `initTimelineTooltip()`/`showMCSequences()` non modifiés — vérifié que les hit areas produites par la nouvelle `drawTimeline()` gardent exactement les mêmes champs (`type`/`x`/`y`/`w`/`h`/`label`/`text`/`positif`/`rawPos`/`startRaw`/`endRaw`/`seqRows`). Tooltip et clic testés fonctionnels en conditions réelles (pas seulement supposés par lecture de code).
- **`#timeline-scores`** : non touché, re-vérifié affichant "Final : X-Y" correctement sur 3 matchs.
- **`detectAllBascules`/`getSortedGoals`/`normPos`** : non modifiés, diff vérifié limité au rendu Canvas à l'intérieur de `drawTimeline()`.
- **`drawMomentumOverlay` supprimée** : recherche exhaustive de tout appelant résiduel — aucun (elle n'était appelée que depuis `drawTimeline()`, remplacée par la logique fusionnée).
- **DPR/Retina** : simulé ×3, canvas physique exactement 3× le canvas logique, rendu net vérifié par capture d'écran.
- **Redimensionnement** : fenêtre réduite à 900px → canvas redessiné à la bonne largeur, pas de distorsion.
- **Changement de match** : re-testé sur 3 matchs réels au profil de score différent (victoire nette, victoire en remontée, nul serré) — légende, échelle et couleurs recalculées correctement à chaque fois.
- **Navigation générale** : 0 erreur console sur l'ensemble des parcours testés (Developer, QA, E2E, 3 matchs différents).

## Verdict

**RAS** — aucune régression détectée. Checklist mise à jour (nouvelle entrée I26).
