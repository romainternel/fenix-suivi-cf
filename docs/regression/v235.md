# Régression — v235 (STORY-17 : Passe visuelle page Joueurs)

**Agent :** Regression Guardian
**Date :** 2026-08-28
**Changement :** `css/style.css` uniquement — `#joueur-panel` (hero), `#joueur-matches` (card), `.jsub-btn` (échelle "Onglet"), et par cohérence transverse `.an-tab-btn` (même échelle, anticipant STORY-16). Aucune donnée, aucune logique JS touchée.

---

## Analyse de risque

CSS pur, ciblage précis (`#joueur-panel`/`#joueur-matches` par ID, uniques à la page Joueurs). Le seul risque de fuite identifié est la modification de `.an-tab-btn`, partagée avec la page Analyse (créée en STORY-14) — traité ci-dessous par un test de fumée dédié plutôt qu'ignoré.

## Vérification

- C5 Page Joueurs : re-testé en conditions réelles (login staff réel, sélection de 2 joueurs différents — un gardien via session fraîche E2E, un joueur de champ) — fiche, badges, tableau détail par match, terrain SVG tous conformes, données identiques. RAS.
- I1/I10 Onglets Analyse (`.an-tab-btn` modifié) : test de fumée dédié — match sélectionné, 5 onglets affichés, style bold appliqué sans casse de mise en page (`02-smoke-analyse-tabs.png`). Pas de re-test approfondi des critères fonctionnels (canvas/drill-down), déjà couverts par QA-05/E2E-04 et non affectés par un changement de typographie pure.
- Export PDF/PPT (I8) : non re-testé (non touché par cette story — badges PPT distincts vérifiés non modifiés par le Code Reviewer).

## Verdict

**RAS** — aucune régression détectée. Checklist mise à jour (C5).
