# Régression — v237 (STORY-16 : Passe visuelle page Analyse)

**Agent :** Regression Guardian
**Date :** 2026-08-28
**Changement :** `css/style.css` uniquement — `#match-block-section` (hero), `.analyse-section`/`.analyse-card` (surface-card), `.an-tab-btn:focus-visible`, typographie de `.enc-card-mini` consolidée (7 tailles → 3). Aucune donnée, aucune logique JS (`page-analyse.js` non modifié).

---

## Analyse de risque

CSS pur sur des sélecteurs exclusifs à `#page-analyse` (`.analyse-section`/`.analyse-card` vérifiés non utilisés ailleurs) ou ciblés par ID (`#match-block-section`). Seul point de vigilance : la story elle-même signale 3 correctifs mobiles récents comme sensibles, alors que cette story ne touche structurellement aucun sélecteur `.pmf-*`/`.pm-*` — traité par une vérification empirique plutôt qu'une simple lecture du diff.

## Vérification

- I1 Page Analyse : re-testée en conditions réelles sur 2 matchs différents (dont un non testé par le Developer) — bloc hero, 5 onglets, RAS.
- I6 Familles d'enclenchement : camembert, matrice 2×2, toggle Attaque/Défense, drill-down par intention testés — RAS. Consolidation typographique des cards validée visuellement (3 niveaux au lieu de ~7).
- Canvas Impact (mode joueur) pleine largeur en premier : re-vérifié à 375px — RAS, comme attendu vu l'absence de recoupement de sélecteurs.
- Header sticky Stats Match non masqué après scroll : re-vérifié à 375px — RAS.
- État vide "Importe un fichier Excel..." (`#match-block-empty`) : re-vérifié avec données effacées — s'affiche correctement dans le bloc désormais en élévation hero.

## Verdict

**RAS** — aucune régression détectée. Checklist mise à jour (I1, I6).
