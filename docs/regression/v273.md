# Régression — v273 (STORY-41 : terrain en accordéon replié + option Météo)

**Agent :** Regression Guardian
**Date :** 2026-09-09
**Changement :** `FENIX-HANDBALL-CF-SUIVI.html` (`#match-block-section` restructuré en accordéon, toggle Nuage de tirs/Météo, `drawTerrain()` devenue dispatcher, nouvelles `drawTerrainDots()`/`drawTerrainMeteo()`), `css/style.css` (`.terrain-accordion-*`, `.terrain-mode-*`), `CLAUDE.md` (v272 → v273)

---

## Analyse de risque

Zone modifiée = le bloc le plus visible de la page Analyse (toujours affiché avant les onglets), avec un changement structurel (ajout d'un accordéon) sur un contenu qui était fixe depuis le début du projet. Risque principal : casser le rendu du nuage de tirs existant en le rendant conditionnel à l'ouverture de l'accordéon (canvas dimensionné à 0×0 pendant que fermé).

## Vérification

- **Nuage de tirs existant** : re-testé sur 2 matchs après un cycle fermé→ouvert — canvas correctement dimensionné (391×243 mesuré), rendu identique à avant la story (mêmes marqueurs But/Tir raté/PB/PO/Jet franc).
- **Cartes comparatives FENIX/Adversaire et bouton TOTAUX** : non touchés par la story (contenu et logique de `updateMatchPage()` inchangés en dehors de l'appel à `drawTerrain()` déjà existant) — re-testés, fonctionnels à l'identique.
- **Bug trouvé et corrigé avant la remise QA** : le chevron d'accordéon faisait pivoter le texte "Replier" avec le symbole (texte affiché à l'envers) — corrigé en séparant symbole et libellé dans deux éléments distincts, re-testé, confirmé propre sur 2 matchs.
- **Mode Météo persistant au changement de filtre** : testé explicitement (bascule Club → FENIX avec Météo actif) — le mode reste actif, le rendu se recalcule sur les données filtrées (pas un cache figé sur l'ancien filtre).
- **Échantillon faible (Risk R4)** : vérifié programmatiquement sur des données réelles que les deux branches (< 5 tirs atténué, ≥ 5 tirs plein) sont bien exercées, pas seulement supposées correctes par lecture de code.
- **Navigation générale** : 0 erreur console sur l'ensemble des parcours testés (Developer, QA, E2E, 2 matchs différents, changement de filtre, cycle replié/déplié, changement de mode dans les deux sens).

## Verdict

**RAS** — aucune régression détectée (le bug du chevron a été trouvé et corrigé avant la remise QA, pas après). Checklist mise à jour (nouvelle entrée I25).
