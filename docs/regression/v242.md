# Régression — v242 (STORY-23 : Migration des données locales + amorçage des familles)

**Agent :** Regression Guardian
**Date :** 2026-08-31
**Changement :** `FENIX-HANDBALL-CF-SUIVI.html` (overlay F5, 2 lignes ajoutées dans `checkLogin()` et l'IIFE de restauration de session, 1 entrée menu Outils), `css/style.css` (+styles F5), `js/supabase-client.js` (+5 fonctions), nouveau `supabase/seed-famille-mapping.sql`

---

## Analyse de risque

Les seuls points de contact avec du code existant sont `checkLogin()` (2 lignes ajoutées, aucune modifiée) et l'IIFE de restauration de session (logique existante préservée, juste enrichie d'un appel conditionnel). Le reste est entièrement additif (nouvelle fonction, nouvel overlay, nouvelle entrée de menu). Risque de régression croisée structurellement faible — le risque réel de cette story est l'étanchéité staff/joueur (déjà couvert en détail par QA-14/E2E-12), pas une régression sur l'existant.

## Vérification

- **C1 (Authentification Staff)** : connexion "Partage" retestée à plusieurs reprises pendant les tests de cette story (avec et sans données locales à migrer) — fonctionne comme avant, `checkAndOfferLocalMigration()` n'interfère jamais avec le flux de connexion lui-même.
- **C2 (Authentification Joueur)** : connexion joueur retestée explicitement — bascule en Mode Lecture Joueur inchangée, et confirmé que le prompt de migration ne s'affiche jamais dans ce contexte (nouveau comportement attendu, pas une régression mais le comportement correct recherché).
- **I7 (Gestion comptes joueurs)** et **I9 (Menu Outils)** : le menu "⚙ Outils" a une entrée de plus (`#btn-migrate-local`) — les 2 entrées existantes (`Comptes joueurs`, `Vue joueur`) toujours fonctionnelles, ouverture/fermeture du menu inchangée.

## Verdict

**RAS** — aucune régression détectée.
