# Régression — v271 (STORY-39 : bloc "Essentiel" en tête de page, match + saison)

**Agent :** Regression Guardian
**Date :** 2026-09-08
**Changement :** `FENIX-HANDBALL-CF-SUIVI.html` (nouveaux conteneurs `#essentiel-match`/`#essentiel-saison`, retrait de la carte `.ia-card`/`#ia-analyse` et de `.analyse-grid` dans l'onglet Résumé), `js/page-analyse.js` (`generateResume3Points()` écrit dans `#essentiel-match`, `generateSeasonCorrelations()` alimente `#essentiel-saison` via la nouvelle `_renderEssentielSaison()`, `updateAnalysePage()` bascule les deux blocs), `css/style.css` (`.essentiel*`, simplification de `.ia-diagnostic`, retrait de `.analyse-grid`/`.ia-card .analyse-card-header` devenues mortes)

---

## Analyse de risque

Zone modifiée = la structure HTML de l'onglet Résumé (retrait d'une carte + de son wrapper en grille) et le point d'écriture de 2 fonctions déjà utilisées ailleurs (`generateResume3Points` alimente aussi le candidat "Supériorités" partagé avec STORY-42, `generateSeasonCorrelations` alimente aussi le tableau de corrélations existant). Risque principal : casser le tableau de corrélations ou l'affichage Coach en modifiant leurs voisins immédiats dans le DOM.

## Vérification

- **Carte Coach (I13)** : re-testée visuellement — toujours présente, seule dans sa carte (plus dans une grille à 2 colonnes à moitié vide), textarea et bouton de sauvegarde fonctionnels à l'identique.
- **Tableau de corrélations saison** : `generateSeasonCorrelations()` a été étendue (nouvelle collecte `signauxForts`) mais son bloc `container.innerHTML = ...` (le tableau lui-même) n'a pas été touché — re-vérifié à l'écran, rendu identique à avant (même lignes, mêmes icônes 🔑/↑↓, même légende).
- **Aucune référence résiduelle** aux éléments retirés : recherche exhaustive (`grep`) de `ia-analyse`, `ia-card`, `analyse-grid` sur tout le projet — seules des mentions dans la documentation (attendu), aucune dans le code.
- **Basculement vue match ↔ vue saison** : re-testé sur 2 matchs réels différents (un remporté par l'adversaire de justesse, un avec un écart plus large) — `#essentiel-match`/`#essentiel-saison` toujours en miroir exact de `#analyse-content`/`#analyse-empty`, jamais les deux visibles en même temps ni les deux masqués.
- **Non-régression onglets (I10)** : les 5 onglets restent accessibles et fonctionnels, `sessionStorage.an_active_tab` non affecté (aucun changement dans `_analyseTab`/`AN_TABS` par cette story).
- **Non-régression indicateurs clés (I23, v270)** : toujours fonctionnels, testés dans le même passage (jauge de rythme + carte Supériorités visibles sous le bloc Coach).
- **Navigation générale** : 0 erreur console sur l'ensemble des parcours testés (Developer, QA, E2E, 2 matchs + vue saison + cas limite <3 matchs simulé).

## Verdict

**RAS** — aucune régression détectée. Checklist mise à jour (nouvelle entrée I24).
