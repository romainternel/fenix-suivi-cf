# Régression — v240 (STORY-21 : Import Excel → Supabase)

**Agent :** Regression Guardian
**Date :** 2026-08-31
**Changement :** `FENIX-HANDBALL-CF-SUIVI.html` (`processFile()`, ajout d'une synchronisation Supabase en fin de fonction), `js/supabase-client.js` (+`buildMatchDataRows()` et sa table de correspondance).

---

## Analyse de risque

Le parsing local (`DATA`, `JOUEURS_TERRAIN`, `TEMPS_JEU`, `_rawBilanRows`) et le rendu qui en dépend (Dashboard, Analyse, Joueurs, Notes, Impact) ne sont pas modifiés — seul un bloc de synchronisation est ajouté après que ces étapes se sont déjà déroulées normalement. Risque de régression sur l'existant structurellement faible ; le risque réel de cette story porte sur la synchronisation elle-même (déjà couvert par QA-12/E2E-10), pas sur une régression croisée.

## Vérification

- **C3 (Import fichier Excel)** : re-testé en conditions réelles (3 imports au total sur ce cycle, dont un depuis une session E2E indépendante) — `DATA`/`MATCHS`/`SAISONS`/`JOUEURS_TERRAIN` toujours peuplés correctement, dashboard affiche des stats non nulles et identiques à avant ce changement.
- **C4 (Dashboard staff)** : chiffres re-vérifiés identiques avant/après (27/48, 30/54, 56%, etc.) sur les 3 imports.
- Aucune autre feature de la checklist ne dépend de `processFile()` d'une manière qui serait affectée par un ajout en fin de fonction.

## Verdict

**RAS** — aucune régression détectée. La checklist elle-même n'a pas besoin de mise à jour (C3/C4 déjà présents avec leurs critères existants, toujours vérifiés vrais) — la synchronisation Supabase n'est pas encore une feature utilisateur observable indépendamment (cf. `docs/regression/v239.md`), elle le deviendra avec STORY-22.
