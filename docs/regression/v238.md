# Régression — v238 (STORY-19 : Panneau latéral Comptes / Vue joueur)

**Agent :** Regression Guardian
**Date :** 2026-08-28
**Changement :** `FENIX-HANDBALL-CF-SUIVI.html` (restructuration DOM des 2 modales), `css/style.css` (nouvelles classes `.slide-panel`/`.slide-panel-overlay`, suppression d'une règle `#pa-modal` morte en conflit), `js/player-mode.js` (mécanique d'ouverture/fermeture + focus trap + Escape).

---

## Analyse de risque

Le risque principal de cette story est fonctionnel, pas seulement visuel : le mécanisme d'authentification joueur (C2, Critique) dépend du formulaire à l'intérieur du panneau restructuré. Traité en priorité par un test de bout en bout du cycle complet (création réelle d'un compte → déconnexion → reconnexion avec le mot de passe réel), pas seulement une vérification visuelle de l'ouverture/fermeture.

## Vérification

- **C2 Authentification Joueur** : cycle complet re-testé — compte créé via le nouveau panneau "Comptes joueurs" (joueur Isaac Maurice, mot de passe réel), déconnexion, reconnexion au clavier avec ce mot de passe → bascule correcte en Mode Lecture Joueur avec le bon nom et les bonnes données. RAS.
- **I7 Gestion comptes joueurs** : création + suppression testées de bout en bout à travers le nouveau panneau. RAS.
- **I9 Menu "⚙ Outils"** : re-smoke-testé (ouverture/fermeture du menu, clic sur les 2 entrées) — aucune régression, le déclencheur n'a pas été modifié par cette story.
- **I11 Panneau Vue joueur** (nouvelle ligne) : sélection joueur + démarrage preview + retour staff testés de bout en bout sur 2 joueurs différents (dont un en session E2E fraîche).
- **Bug trouvé pendant le développement** : une règle CSS `#pa-modal{display:none}` morte (résidu d'une itération antérieure, jamais nettoyée) empêchait le panneau Comptes joueurs de s'afficher après le passage à `classList`. Corrigée par le Developer avant la fin de l'implémentation — voir Code Review pour le détail. Sans cette correction, la story aurait été bloquante ; avec, aucun résidu détecté dans les tests QA/E2E qui ont suivi.
- **Autres modales du fichier** (`#gb-zone-modal`, `#notes-detail-modal`, `#player-modal`, `#graph-modal`, `#eff-info-modal`) : ni CSS ni JS modifiés, aucun sélecteur partagé avec `.slide-panel` — non re-testées en profondeur, risque de fuite structurellement nul.

## Verdict

**RAS** — aucune régression détectée (après correction du bug trouvé en cours d'implémentation, qui n'a jamais atteint la QA/E2E). Checklist mise à jour (C2, I7, I9, nouvelle ligne I11).
