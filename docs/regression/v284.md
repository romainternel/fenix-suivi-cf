# Régression — v284 (STORY-48 : Mode "Articulation" côté Attaque)

**Agent :** Regression Guardian
**Date :** 2026-09-16
**Changement :** `js/page-analyse.js` (+constantes, +14 fonctions, 3 modifications ciblées), `FENIX-HANDBALL-CF-SUIVI.html` (v284), `CLAUDE.md` (v284)

---

## Analyse de risque

Feature additive côté rendu (nouvelle fonction de dessin + son état), avec 3 modifications chirurgicales du code partagé existant (bouton, `_setEncTeamMode`, `_setEncGraphMode`). Risque principal : casser le mode Défense en touchant le code partagé — mitigé par construction, vérifié ci-dessous plutôt que supposé.

**Limite assumée de cette vérification** : le serveur MCP Playwright est indisponible (`CONNECT_TIMEOUT`) depuis le début de ce cycle. Aucune re-vérification par clic réel du mode Défense n'a été possible — la non-régression ci-dessous est établie par lecture de code (`git diff`) et par le fait qu'aucune fonction défense n'a de dépendance vers le nouveau code, pas par un test observé dans un navigateur. Checklist (I22) mise à jour pour refléter honnêtement cette limite.

## Vérification

- **Zéro ligne défense modifiée** : `git diff js/page-analyse.js` ne contient que 3 suppressions, toutes correspondant aux points d'intégration autorisés par l'Architecture (bouton "🎯 Articulation", 2 lignes de `_setEncTeamMode`, dispatch dans `_setEncGraphMode`). Aucune ligne de `computeArticulationStats`, `_articTauxDefense`, `_articDefClass`, `_articArcY`, `ARTIC_LAYOUTS`, `_articCourtSvg`, `_articPrimaryEntry`, `ARTIC_BLOCKS`, `_articBlockEff`, `_articBlockDetail`, `computeArticCombos`, `_articRankedCombos`, `_drawArticulationCourt`, `_redrawArticCourt`, `_setArticDispositif`, `_setArticWidth`, `_setArticManualJoueur`, `_setArticManualCombo`, `_toggleArticPosteEditor` n'apparaît dans le diff.
- **État isolé** : recherche croisée confirmant qu'aucune variable `window._articAtt*` n'apparaît dans le bloc défense et qu'aucune variable `window._artic*` (défense) n'apparaît dans le nouveau bloc attaque — les deux côtés ne peuvent pas se marcher dessus par construction.
- **CSS inchangé** : `git diff css/style.css` vide (aucune modification), conforme à l'Architecture §6. Les 32 classes référencées par le nouveau bloc existent déjà toutes (vérifié par grep croisé) — la règle `.enc-pie-mode-btn.artic-disabled` devient CSS mort (plus jamais posée par le JS) mais ça n'affecte aucun rendu, retrait hors scope de cette story.
- **Import/Supabase non touchés** : `git diff` ne montre aucun changement sur `supabase/schema.sql`, `js/supabase-client.js` — cette story ne fait que lire les colonnes déjà importées par STORY-47, aucun risque sur le pipeline d'import.
- **Syntaxe** : `node --check js/page-analyse.js` passe sans erreur.
- **Calcul non-régressif ailleurs** : `computeArticulationAttStats` et consorts sont des fonctions entièrement nouvelles, appelées uniquement depuis le nouveau chemin (`_drawArticulationAttCourt`, atteint uniquement quand `mode === 'articulation' && window._encTeamMode !== 'adv'`) — aucun appelant existant modifié pour les invoquer, donc aucun risque de changer un calcul déjà affiché ailleurs dans l'app.
- **Checklist mise à jour** : I22 (défense, limite Playwright documentée honnêtement) et nouvelle entrée I30 (attaque, réserve E2E documentée).

## Verdict

**RAS sur le code partagé** (preuve par `git diff`, pas de régression possible sur le mode Défense ni sur l'import). **AUDIT INCOMPLET sur le nouveau code lui-même** — la nouvelle interface (I30) n'a pas pu être cliquée en conditions réelles (MCP Playwright indisponible), voir `docs/e2e/E2E-48-mode-articulation-attaque.md` pour le script de vérification manuelle remis à Romain. Ce cycle ne bloque pas la mise en production (le code est poussé, comme toujours dans ce projet) mais la story reste réservée tant que Romain n'a pas confirmé le script de la section E2E.
