# Régression — v282 (STORY-47 : Import "Articulation offensive")

**Agent :** Regression Guardian
**Date :** 2026-09-16
**Changement :** `supabase/schema.sql` (+7 colonnes `match_data`), `js/supabase-client.js` (`DATA_HEADER_TO_COLUMN`/`MATCH_DATA_COLUMN_ORDER`), `FENIX-HANDBALL-CF-SUIVI.html` (`COLS`), `CLAUDE.md` (v282)

---

## Analyse de risque

Changement purement additif à la couche import (nouvelles colonnes en fin de mapping, aucune colonne existante déplacée ni renommée). Risque principal identifié en amont (Risk Analyst, `docs/risks/articulation-attaque.md` R2) : une dépendance d'ordre stricte avec une migration SQL manuelle sur la production — ce risque s'est concrétisé une fois (import tenté avant rechargement du navigateur après déploiement), mais sans aucune casse : le reste de l'import (29 colonnes existantes) a continué de fonctionner normalement pendant l'incident, confirmant que l'ajout est bien isolé et n'a pas de mode d'échec partagé avec l'existant.

## Vérification

- **Non-régression de l'import existant** : après le réimport réel en production par Romain, le nombre total de lignes (350) et de matchs (J01 BILLERE-FENIX, J02 FENIX-LA CRAU) est resté cohérent avec l'état d'avant ce cycle — aucune perte ni duplication de données sur les colonnes déjà en place.
- **Nouvelles colonnes** : 120 lignes `match_data` portent désormais `articulation_att` non nul, valeurs vérifiées identiques à la lecture directe du fichier Excel source (cf. QA-47).
- **Round-trip `DATA`/`COLS`** : vérifié par un script reproduisant exactement `rowToPositionalArray`/`MATCH_DATA_COLUMN_ORDER` sur des lignes réelles fraîches — aucune divergence entre la valeur nommée retournée par Supabase et la valeur lue au bon index positionnel.
- **Résilience ancien format** : non re-testée par un nouvel import (éviter une action destructive supplémentaire sans nécessité), confirmée par lecture de code — mapping générique par nom d'en-tête, comportement identique et déjà éprouvé pour `articulation_def`/`p1`-`p6` (STORY-33, I21).
- **Aucun affichage encore branché sur cette donnée** (STORY-48 à venir) : le bouton "🎯 Articulation" reste désactivé en mode Attaque, comportement inchangé et attendu à ce stade — confirmé par une capture d'écran réelle de Romain (bouton grisé, comme avant ce cycle).
- **Navigation générale** : aucune anomalie rapportée sur le Dashboard/page Analyse pendant les échanges de vérification (capture d'écran fournie par Romain : cartes familles + camembert cohérents).

## Verdict

**RAS** — aucune régression détectée. L'unique incident (import sans effet sur les nouvelles colonnes) était dû à un cache navigateur non rafraîchi côté utilisateur, pas à un défaut du code — résolu par un rechargement forcé, documenté dans QA-47 pour référence future. Checklist mise à jour (nouvelle entrée I29).
