# Régression — v257 (STORY-33 : import "Articulation défensive")

**Agent :** Regression Guardian
**Date :** 2026-09-02
**Changement :** `FENIX-HANDBALL-CF-SUIVI.html` (`COLS` +7), `js/supabase-client.js` (`DATA_HEADER_TO_COLUMN`/`MATCH_DATA_COLUMN_ORDER` +7), `supabase/schema.sql` + nouvelle migration (`match_data` +7 colonnes)

---

## Analyse de risque

Extension purement additive de structures existantes (aucune colonne retirée ou renommée, aucune fonction de rendu modifiée) — risque de régression a priori faible. Point réel à risque : le pipeline d'import lui-même (`buildMatchDataRows`), partagé par TOUTES les colonnes existantes, pas seulement les nouvelles.

## Vérification

- **C3 (Import Excel)** : réimport réel en production (v257) du fichier Excel à jour de Romain — 540 lignes, 4 matchs (contre 539/3 avant), 166 lignes avec les nouvelles colonnes renseignées, vérifiées jusqu'en base via requête REST indépendante. 0 erreur console.
- **Non-régression des autres colonnes déjà en place** : les 22 colonnes existantes toujours correctement peuplées après ce réimport (vérifié par échantillonnage sur les lignes retournées).
- **Navigation générale** (Dashboard/Analyse/Joueurs) après le réimport : testée, 21 joueurs toujours corrects, 3 vrais matchs + 1 entrée parasite préexistante ("Rencontre", cf. note qualité de donnée ci-dessous) dans le sélecteur, 0 erreur console.

## Effet de bord réel — nouvelles données en production

Le réimport de test a chargé le fichier Excel à jour de Romain, qui contenait un 4ᵉ match ("AMICAL FENIX-LIMOGES") non encore présent en production. Comportement voulu du système (réimport = remplacement complet), déjà rencontré et confirmé acceptable par Romain lors d'un précédent audit similaire.

## Observation qualité de donnée (préexistante, pas une régression de cette story)

Le fichier Excel contient une ligne "légende" avec des libellés numérotés (`"#1 Club"`, `"#2 Phase att"`...) juste après la vraie ligne d'en-tête. Cette ligne n'a que 22 colonnes (antérieure à l'ajout des 7 nouvelles) et possède une valeur non vide dans la colonne `Rencontre` ("Rencontre" littéralement), donc elle passe le filtre d'import et apparaît comme un faux match dans le sélecteur de la page Analyse. Comportement du filtre `row[COLS.rencontre]` inchangé par cette story — présent avant, découvert à cette occasion. À signaler à Romain pour un nettoyage de son fichier source (supprimer cette ligne), pas une action corrective côté code.

## Verdict

**RAS** — aucune régression détectée. Nouvelle feature ajoutée à la checklist (I21).
