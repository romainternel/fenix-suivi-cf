# Régression — v270 (STORY-42 : indicateurs clés — rythme du match et recalcul des Supériorités numériques)

**Agent :** Regression Guardian
**Date :** 2026-09-08
**Changement :** `js/page-analyse.js` (nouvelle `computeSuperiorites()`, réécriture de la carte "Possessions" en jauge de rythme et de la carte "Supériorités" dans `generateIndicateurs()`, mise à jour du candidat "Supériorités" dans `generateResume3Points()`, correction du "Bilan" de la réponse Chat IA "Supériorités numériques" dans `generateChatResponse()`), `css/style.css` (`.tempo-*`, `.sup-*`, `.info-i`), `FENIX-HANDBALL-CF-SUIVI.html` + `CLAUDE.md` (bump `v269` → `v270`)

---

## Analyse de risque

Zone modifiée = 3 fonctions distinctes de `js/page-analyse.js` qui partageaient (avant cette story) 3 copies indépendantes du même calcul bugué de "Supériorités numériques" — le risque principal était qu'une story ciblant seulement 2 des 3 sites (comme le prévoyait initialement l'Architecture) laisse le 3e (Chat IA) afficher un bilan différent de celui des cartes, une incohérence immédiatement visible pour Romain. Risque secondaire (R1 du Risk Analyst) : un match sans `phase_att` renseigné affichant un "0 — 0" trompeur au lieu d'un message explicite.

## Vérification

- **3 sites identifiés, 3 corrigés** : recherche exhaustive (`grep`) de l'ancien pattern `(r[COLS.phase_att] || '').toString().includes(...)` sur tout le projet — un 3e site non prévu par l'Architecture trouvé dans `generateChatResponse()` (réponse "Supériorités numériques" du Chat IA) et corrigé pendant cette passe de non-régression (cf. `docs/code-review/STORY-42.md`, addendum). Plus aucune occurrence de l'ancien calcul dupliqué dans le code.
- **Cohérence inter-fonctions** : vérifiée explicitement en conditions réelles sur "J01 BILLERE-FENIX" — carte Indicateurs clés "FENIX 6 — 5 Adversaire" et réponse Chat IA "Bilan : ✅ FENIX gagne les supériorités" (6-5 dans le détail) parfaitement cohérents entre eux après correction.
- **Comparaison ancien/nouveau calcul sur un vrai match** (Risk R3) : documentée dans `docs/qa/QA-42-indicateurs-rythme-superiorites.md` — l'ancien calcul aurait affiché "6b/10t vs 1b/1t" (lecture : écrasante domination), le nouveau "FENIX 6 — 5 Adversaire" (lecture : bilan serré). Écart significatif à signaler à Romain, ce n'est pas une régression.
- **Cas limite `phase_att` absent** (R1) : testé avec un jeu de données synthétique (copie d'un vrai match, `phase_att` vidé) → "Non disponible pour ce match" affiché, aucune classe avantage/désavantage appliquée par erreur.
- **Cas limite "séquences taguées sans tir"** (trouvé en marge, pas anticipé par la story) : sur "AMICAL FENIX-LIMOGES" (2 lignes taguées, ni but ni tir raté), affichage "FENIX 0 — 0 Adversaire" / "0/0 possessions (0%)" — pas de `NaN`, division par zéro bien gardée. Distinction correcte avec le cas "Non disponible" (ici les données existent, juste aucun tir dans la fenêtre). Cosmétique mineur noté au QA, pas bloquant.
- **Non-régression cartes Efficacité/Pertes de balle/Buts/Tirs** : rendu identique à avant (capture d'écran comparée), MT1/MT2 toujours affichés pour ces 4 cartes.
- **Non-régression bloc terrain/comparatif et onglets** (I1/I10) : re-vérifiés visuellement pendant le passage E2E, aucun impact — la story ne touche ni la structure des onglets ni le terrain.
- **Navigation générale** : 0 erreur console sur l'ensemble des parcours testés (QA + E2E, 3 matchs réels différents + 2 jeux de données synthétiques).

## Verdict

**RAS** — aucune régression détectée, une 3e occurrence du bug corrigé a été trouvée et neutralisée avant mise en production plutôt que découverte après coup. Checklist mise à jour (nouvelle entrée I23).
