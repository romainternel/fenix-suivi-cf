# Régression — v276 (STORY-45 : Matrice 2×2 — refonte visuelle)

**Agent :** Regression Guardian
**Date :** 2026-09-09
**Changement :** `js/page-analyse.js` (`_drawEncMatrix()` enrichie visuellement, `_initEncMatrixHover()` étendue pour l'agrandissement au survol), `CLAUDE.md` (v275 → v276)

---

## Analyse de risque

Zone modifiée = une visualisation Canvas 2D interactive (hover + clic vers le drill-down famille), avec un risque de confusion initial entre deux fonctions au nom proche (`_drawEncRadar()` dead code vs `_drawEncMatrix()` réellement utilisée) — déjà résolu par une vérification préalable avant le développement.

## Vérification

- **Bonne fonction ciblée** : confirmé par recherche exhaustive que `_drawEncRadar()` n'a aucun appelant (dead code préexistant, non lié à cette story, laissé intact) et que `_drawEncMatrix()` est bien la fonction derrière le bouton "Matrice 2×2".
- **Positions des bulles inchangées** : `xS`/`yS`/calcul fréquence-efficacité non touchés — vérifié par diff, seul le rendu (couleurs, ombre, libellés, survol) a changé.
- **2 bugs visuels trouvés et corrigés avant la remise QA finale** : chevauchement de libellés adjacents (marge de collision insuffisante) et débordement de texte hors-canvas en bord de graphique (texte centré non bridé) — les deux corrigés, re-testés sur 3 matchs différents + un cas saison à 8 familles (le plus dense rencontré).
- **Interaction préservée** : `window._encMatrixDots` garde le même contrat (x/y/famille/eff/possessions/utilisPct) consommé par `_initEncMatrixHover()` (tooltip + clic vers `_selectEncFamille`) — clic re-testé fonctionnel après le changement.
- **Non-régression des autres modes** : Vue générale (camembert), Articulation, bascule Attaque/Défense, vue saison — tous re-testés fonctionnels après le changement, dans le même parcours que le nouveau mode Matrice.
- **Navigation générale** : 0 erreur console sur l'ensemble des parcours testés (Developer, QA, E2E, 3 matchs différents + vue saison).

## Verdict

**RAS** — aucune régression détectée (les 2 bugs visuels ont été trouvés et corrigés avant la remise QA finale, pas après mise en production). Checklist mise à jour (nouvelle entrée I27).
