# Régression — v241 (STORY-22 : Lecture depuis Supabase au démarrage)

**Agent :** Regression Guardian
**Date :** 2026-08-31
**Changement :** `FENIX-HANDBALL-CF-SUIVI.html` (suppression de `loadFromLocalStorage()`, remplacée par `loadFromSupabase()` + overlay chargement/erreur), `css/style.css` (styles overlay), `js/supabase-client.js` (+`rowToPositionalArray()`, `MATCH_DATA_COLUMN_ORDER`)

---

## Analyse de risque

Changement structurellement différent des précédents de ce cycle (STORY-20/21, purement additifs) : ici, la fonction de chargement initiale de **toute** l'app est remplacée. Toute feature qui dépend de `DATA`/`JOUEURS_TERRAIN`/`TEMPS_JEU`/`_rawBilanRows`/`MATCHS`/`SAISONS` — c'est-à-dire pratiquement toute l'application (Dashboard, Analyse, Joueurs, Notes, Impact, mode joueur) — est potentiellement affectée si la reconstruction de ces globales depuis les lignes Supabase diverge, même légèrement, de la reconstruction depuis le parsing Excel local. C'est le risque P0 R1 documenté depuis l'Architecture (`docs/risks/migration-supabase.md`), la story existe spécifiquement pour le mitiger.

## Vérification

- **C3/C4 (Import, Dashboard)** : re-testé après import réel du fichier de référence — chiffres identiques aux valeurs de référence établies dans la session.
- **C5 (Joueurs — terrain + fiche)** : re-testé sur device sans aucune donnée locale — fiche Antonin Vache (2/4, 50%) et fiche Jules Gougeon (0/0, le cas exact du bug "Jules F" original) toutes deux correctes.
- **I1 (Analyse — enclenchements)** : camembert 8 familles, somme des % = 100% — signal fort de non-divergence de l'ordre des colonnes.
- **I2 (Notes)** : 12 joueurs, cohérence interne des totaux vérifiée ligne par ligne.
- **Cas d'erreur** : panne réseau simulée → écran d'erreur clair, pas de plantage ni de rendu partiel ; récupération complète au clic sur "Réessayer".
- **Vérification structurelle du point critique** : `MATCH_DATA_COLUMN_ORDER` comparé index par index à `COLS` (22/22 identiques) — cf. `docs/code-review/STORY-22.md`.

## Verdict

**RAS** — aucune régression détectée. Le scénario "Jules F" qui a motivé toute cette migration est confirmé résolu : un appareil sans aucune donnée locale affiche désormais les données à jour dès l'ouverture, y compris pour un joueur récemment ajouté sans encore de statistiques.
