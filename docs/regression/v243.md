# Régression — v243 (STORY-24 : Rebranchement comptes joueurs / notes coach / lecture famille)

**Agent :** Regression Guardian
**Date :** 2026-08-31
**Changement :** `FENIX-HANDBALL-CF-SUIVI.html` (`loadFromSupabase()` étendue à 7 tables, écran de connexion + `checkLogin()` async), `js/player-mode.js` (comptes joueurs réécrits), `js/page-analyse.js` (`getEncFamille()`/`saveCoachAnalyse()`), `js/supabase-client.js` (+3 fonctions), nouvelle Edge Function `delete-player-account`

---

## Analyse de risque

Story la plus étendue en surface de code depuis STORY-22 : touche l'authentification (les deux rôles, staff et joueur), la gestion des comptes, les notes de coach et la classification des familles tactiques. Risque de régression réel sur plusieurs features de la checklist (C1, C2, I1, I7) — traité en priorité.

## Vérification

- **C1 (Authentification Staff)** : re-testé, comportement identique malgré le passage de `checkLogin()` en fonction `async`.
- **C2 (Authentification Joueur)** : cycle complet re-testé (création, connexion, mauvais mot de passe, nom manquant, suppression) — tous corrects avec la nouvelle implémentation Supabase Auth.
- **I1 (Page Analyse — familles)** : camembert re-vérifié après bascule de la source de classification (`ENC_FAMILLE_MAP` → `FAMILLE_MAPPING`) — valeurs strictement identiques à la dernière vérification (STORY-22), confirmant qu'aucune divergence n'a été introduite par le changement de source.
- **I7 (Comptes joueurs)** : panneau re-testé avec la nouvelle source (`player_profiles`) — plus de mot de passe affiché (changement intentionnel, pas une régression), création/suppression fonctionnelles.
- **I2 (Page Notes — actions ATT/DEF)** : non concernée par cette story (feature distincte de la note libre de coach, qui elle est concernée — cf. nouvelle entrée I13) ; non re-testée spécifiquement, aucun code partagé modifié.
- **I13 (nouvelle entrée) — Note libre du coach** : testé explicitement cross-appareil, comportement conforme au critère d'acceptation central de la story.

## Verdict

**RAS** — aucune régression détectée. Toutes les features re-testées se comportent comme avant (ou mieux, pour C2/I7 où la sécurité s'améliore) malgré le changement de source de données.
