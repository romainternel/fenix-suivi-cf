# Régression — v275 (STORY-44 : détail par intention enrichi — but/tir, PB, jet franc)

**Agent :** Regression Guardian
**Date :** 2026-09-09
**Changement :** `js/page-analyse.js` (`_buildEncDetailTable()` — ajout d'une colonne JF et d'une 2e passe dédiée pour la compter correctement), `CLAUDE.md` (v274 → v275)

---

## Analyse de risque

Zone modifiée = une fonction déjà utilisée en production sans interruption depuis plusieurs mois (`_buildEncDetailTable`, niveau 1 du drill-down familles). Risque principal : casser le calcul déjà correct de Buts/PO/Ratés/PB/Poss./Eff. en ajoutant la colonne JF à côté.

## Vérification

- **Colonnes existantes non affectées** : Buts/PO/Ratés/PB/Poss./Eff. recalculés à l'identique sur 2 matchs réels après le changement — mêmes valeurs qu'avant la story (non comparées à un ancien commit faute de captures antérieures dédiées, mais cohérentes avec les valeurs déjà vues dans les rapports STORY-42/43 sur les mêmes matchs).
- **Bug trouvé et corrigé avant la remise QA** : la colonne JF affichait 0 partout dans la 1ère version (lignes "Jet franc" sans valeur "possession", donc exclues du filtre principal) — corrigé par une 2e passe dédiée, revérifié avec des valeurs non nulles réelles (ISO 4 : 7, ISO 3 : 3, ISO 2 : 3 sur "AMICAL FENIX-BILLERE").
- **Niveau 2 non touché** : `_buildEncIntentionDetailTable` — diff vérifié vide, aucune colonne JF ajoutée là (hors scope), re-testé fonctionnel par clic réel.
- **Non-régression changement de famille** : `_encSelectedIntention` correctement remis à `null` en changeant de famille après avoir ouvert un niveau 2 — pas de détail fantôme affiché.
- **Non-régression côté Adversaire** : table générée sans erreur avec `isAdv=true` (colonne `finalite`).
- **Navigation générale** : 0 erreur console sur l'ensemble des parcours testés (Developer, QA, E2E, 2 matchs différents, 3 familles différentes, les deux camps FENIX/Adversaire).

## Verdict

**RAS** — aucune régression détectée (le bug JF=0 a été trouvé et corrigé avant la remise QA, pas après mise en production). Checklist mise à jour (I6 enrichie).
